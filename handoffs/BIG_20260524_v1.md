# BIG — Cross-Project Coordinator Session
# Date: 2026-05-24 (session 1 ของวัน)
# Previous: handoffs/archive/BIG_20260523_v1.md

## STATE (snapshot ณ เวลาปิด session)
HORATAD : V3.3.23 — main + deployed ✅
BIBLE   : KB V2.3 active | kb_v24-3.json 290 rules ✅ | รอ Groq/Typhoon extraction (user)
JULIAN  : 58,428/100,000 records — automation รันทุก 6 ชม. ✅
NOK     : Phase 1 deployed (V3.3.14) — Phase 2 ready
GUARD   : Phase 1 6/7 ✅ — P1-E blocked (user R-07 decision)
Sandbox : claude/happy-brahmagupta-BOpuK — main @ ba92026 ✅

---

## DONE (session นี้)

### Lighthouse + CSP coordination (session 2 — 2026-05-24)
✓ **ทบทวน Lighthouse + CSP backlog** — วิเคราะห์ benefit/cost-drawbacks ของ R-08 (Lighthouse) + P2-A (CSP enforce)
✓ **`docs/admin/USER_TASKS_lighthouse_csp_2026-05-24.md`** — user checklist Part 1 (Lighthouse mobile+desktop) + Part 2 (CSP violations collection)
✓ **PROJECT_STATUS.md** — ⭐ tag user tasks ที่ critical + link checklist

### Branch cleanup (session 2 — 2026-05-24)
✓ **ตรวจ merged branches** — 6 claude/* merged เข้า main แล้ว (happy-brahmagupta-BOpuK, horatad-2zwk8, horatad-LDo6g, jolly-mendel-NhFwc, julian-status-report-BHbP0, wonderful-wozniak-aOZEW)
✓ **`scripts/admin/branch_cleanup.mjs` — sandbox detection** — proxy 127.0.0.1 = 403 บน push --delete → script bail out + print 3 ทางเลือก (local CLI / GH workflow / web UI) แทน retry สูญเปล่า 6 ครั้ง
⚠ **ลบจริงไม่ได้ใน sandbox** — ฝาก [ทดลองใช้] user run local หรือรอ cron weekly (ทุก Sun 02:00 ICT — แต่ filter >14 วัน → branches ใหม่กว่านี้รออาทิตย์หน้า)

### ESLint + Prettier setup (session 2 — 2026-05-24)
✓ **`package.json` + lock** — eslint 9.18, prettier 3.4, globals 15.14 (npm 88 packages)
✓ **`eslint.config.mjs`** — flat config; script.js/sw.js/auth-pin.js = browser script, v3/workers/scripts = modules; permissive baseline (no-undef off สำหรับ legacy globals)
✓ **`.prettierrc.json`** — 100 char, single quote, semi, trailing comma es5
✓ **`.prettierignore`** — exclude data/, kb*.json, tools/, *.html, handoffs/archive/
✓ **`.github/workflows/ci.yml`** — เพิ่ม Install + ESLint + Prettier check (Prettier = continue-on-error)
✓ **Real bug fixed by lint baseline** — `scripts/admin/new_project.mjs:301` regex `\*\***GUARD***\*` มี *** unescaped → fixed to `\*\*GUARD\*\*`
✓ Baseline: **0 errors / 82 warnings** (เทียบกับก่อนหน้านี้ 58 — file set ต่างกัน), Prettier 41 files style ไม่ตรง (defer)

### CLAUDE.md trim (session 2 — 2026-05-24)
✓ **807 → 791 บรรทัด** (−16, −2%) — ลบ:
  - Token efficiency #4 "แยก session type" (dead concept — session ใช้ project scope ไม่ใช่ type)
  - "Pending bugs / backlog" stub (2-line pointer ที่ไม่มี value)
⚠ **Trim ลึกกว่านี้** — content ที่เหลือ density สูงทุกบรรทัด ไม่มี dead weight ชัดเจน → ต้อง user review ก่อน batch trim ถัดไป

### Doc simplification (session 1)
✓ **ลด doc surface** — archive BIBLE_MISSION.md + JULIAN_MISSION.md (duplicate ของ memory files)
✓ **CLAUDE.md: Doc management rule** — "doc = Claude's job, not user's" + source of truth hierarchy (5 layers)
✓ **CLAUDE.md: file ownership cleanup** — ลบ docs/BIBLE*.md + docs/JULIAN*.md จาก ownership table
✓ **ECOSYSTEM.md: stale D1 refs** — ลบ "JULIAN D1" ทุกจุด + อัปเดต developer tools section + navigation links

### Context จาก 2026-05-23 (carry over)
✓ Memory files LOG sections (HORATAD/JULIAN/NOK)
✓ Doc audit ครบ 12 ไฟล์
✓ REORG closed + ESLint/Trim CLAUDE.md โอนไป BIG
✓ CLAUDE.md REORG removal

---

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

[ ] ตรวจ BIBLE branch — `git branch -r | grep BIBLE` + ff main ครบไหม
    (BIBLE sessions มักจบจาก context exhaustion → branch ค้างได้)

[ ] **ESLint warnings cleanup** (deferred ของ session นี้) — 82 warnings, mostly unused vars
    - ส่วนใหญ่ argsIgnorePattern `^_` ไม่ครบ — refactor เปลี่ยน `name` → `_name` ที่ unused
    - workers/julian_scraper.mjs no-empty 2 จุด — เพิ่ม comment หรือ throw

[ ] **Prettier --write batch run** — 41 files style ไม่ตรง
    - non-blocking ตอนนี้ (continue-on-error) แต่ทำแล้วประหยัด diff noise ในอนาคต
    - effort: 1 command + manual check 5 นาที

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ตรวจว่า session_scope ใน `.gitignore` หรือเปล่า
    → แนะนำ: track ไว้ เพราะ scope declaration เป็น intent ที่ต้องการ persist

[ ] [ทดลองใช้] ลบ 6 merged branches (sandbox proxy ทำไม่ได้) — รัน local:
    ```
    git push origin --delete claude/happy-brahmagupta-BOpuK
    git push origin --delete claude/horatad-2zwk8
    git push origin --delete claude/horatad-LDo6g
    git push origin --delete claude/jolly-mendel-NhFwc
    git push origin --delete claude/julian-status-report-BHbP0
    git push origin --delete claude/wonderful-wozniak-aOZEW
    ```
    หรือรอ cron stale_branch_cleanup.yml (Sun 02:00 ICT, >14 วัน filter)

---

## WHY LOG

- **ยุบ mission docs (2026-05-24)** — BIBLE_MISSION + JULIAN_MISSION duplicate memory files + handoffs → แก้ที่เดียวไม่ sync ที่อื่น → conflict สะสม; ลบแล้วให้ memory file + ECOSYSTEM.md เป็น source of truth แทน
- **Doc = Claude's job rule** — user ไม่ถนัดเรื่อง doc, ต้องการโฟกัส coding; Claude จัดการ doc เองทั้งหมด ไม่ interrupt user
- **Source of truth hierarchy** — code > memory > ECOSYSTEM > handoff > CLAUDE.md — มีลำดับชัดเจนป้องกันสร้าง layer ใหม่โดยไม่จำเป็น
- **Lighthouse ทำก่อน CSP enforce (2026-05-24)** — Lighthouse cost user 5 นาที, ได้ baseline ใช้เทียบ Phase 2 Step 1 ก่อน/หลัง; CSP enforce ต้อง observation 1 wk + ควร batch กับ core split (touch ไฟล์เดียวกัน) → ทำพร้อมกัน 1 รอบประหยัดกว่า 2 รอบ
- **User checklist เป็น admin coordination output** — แทนที่ใช้ chat message ลอย, BIG เขียน checklist file ใน `docs/admin/USER_TASKS_*.md` → reusable, link จาก PROJECT_STATUS ได้, user เข้ามาดู step-by-step ได้ทุกเมื่อ
- **branch_cleanup.mjs sandbox bail-out (2026-05-24)** — ก่อนหน้านี้ script retry push --delete 6 รอบ failed 403 → noisy + ใช้ token; ปรับ detect 127.0.0.1 proxy ก่อน apply → print แนวทาง 3 ทาง (local/workflow/web UI) → user actionable ทันที — bug pattern: tool ที่ assume sandbox = production environment ทุกตัวควรมี detection guard
- **ESLint baseline พบ real bug (2026-05-24)** — `new_project.mjs:301` regex `\*\***GUARD***\*` parser error (Nothing-to-repeat) ซ่อนตัวอยู่ตั้งแต่ script สร้าง เพราะไม่มี lint mai เคย flag; ESLint baseline catch ทันที → คุ้มค่าการ setup แม้จะ permissive
- **CLAUDE.md trim diminishing returns** — เริ่ม trim เจอว่า content density สูงทุกบรรทัด (807 lines แต่แทบไม่มี dead weight); จับ 2 จุด redundancy ได้แค่ 16 lines (−2%); ตัดสินใจหยุดที่นี่ — เพราะ aggressive trim เสี่ยงลบ rule ที่จำเป็น โดยไม่รู้ ปัญหาจริงคือ rule additions ใหม่ควร reference section เดิมแทน duplicate
