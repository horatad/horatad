# GUARD — Session Handoff (Phase 2 prep + cross-project scan)
# Date: 2026-05-23 (session 3 ของวัน)
# Previous: handoffs/archive/GUARD_20260523_v2.md
# Charter: docs/GUARD_MISSION.md

## STATE
Project   : GUARD
Phase     : 1 (Quick Wins 6/7 ✅) + **Phase 2 prep docs done** (P2-D ✅)
Sandbox   : claude/guard-9KPRp
Commits   : 1 new commit this session (pure docs — no code change)

---

## DONE (session นี้ — autonomous tasks while waiting on user)

### P2-D — CSP Policy doc (deliverable: `docs/cia/csp_policy.md`)
✓ Phase 2 enforcement plan ครบ — Options D/E/F/G analysis
✓ Inline handler inventory: index.html=165, script.js=31, total=196
✓ Refactor strategy (Option E — delegated listeners) เขียนละเอียด step 1-5
✓ Rollback procedure + connect-src maintenance checklist
✓ Conclusion: **Option E recommended** หลัง report-only week ผ่าน

### Cross-project security scan (deliverable: `docs/cia/cross_project_scan_2026-05-23.md`)
✓ **NOK** — pure offline `v3/tts.js`; finding **N-01** TTS data egress (low/informational)
   → ใหม่ **R-19** ใน register (informational only)
✓ **BIBLE** — DSL evaluator `evaluate_applies_when()` **SAFE** ✅ (regex-bounded grammar, no eval)
✓ **BIBLE** — Typhoon prompt construction **NO user text injection vector** ✅ (user name/birth/prov ไม่เข้า prompt)
✓ **REORG** — zero code change, zero security surface; ตรวจ stale doc refs หลัง merge
✓ **JULIAN** delta — target 100K + accuracy A-F field ไม่เพิ่ม risk; R-14 ครอบคลุมอยู่แล้ว

### Charter update
✓ `docs/GUARD_MISSION.md` § 3.2 — เพิ่ม R-19 (TTS egress informational)

### Supplementary scan (added 2026-05-23 session 3 parallel work)
✓ `docs/cia/cross_project_scan_2026-05-23.md` § 2.4 — **Finding B-03 / T-09** (BIBLE dev tool LLM keys ใน localStorage)
  - `tools/kb_extract.html` stores `bible_groq_key` (Groq)
  - `m0_hallucination_test.html` stores `m0_key_g` (Gemini) + `m0_key_r` (Groq) + URL query string
  - **Accept** ภายใต้เงื่อนไข: dev-only, single user, rotation cycle covers
✓ `handoffs/HORATAD_20260522_v3.md` — update F1 XSS DONE (5→7 sites confirmed)
✓ `docs/HORATAD_MANUAL.md` — PIN auth T-06 audit result + DevTools workaround narrative

---

## PENDING — 🟢 Claude ทำเองได้ (Phase 2 prep continues)

[ ] **GUARD-P2-A** — execute Option E refactor (delegated listeners)
    - prerequisite: CSP report-only ผ่าน 1 สัปดาห์ + user collected violations
    - cost: 4-6 hr split across 2 sessions (script.js หนึ่ง, index.html หนึ่ง)
    - effort detail ใน `docs/cia/csp_policy.md` § 5

[ ] **GUARD-P2-B** — script.js code-split (defer Typhoon init, lazy-load JULIAN tank)
    - cross-link to HORATAD M4-M6 rename refactor (high coupling)
    - ทำง่ายขึ้นหลัง HORATAD M4 เสร็จ (natal1→natal naming clean)

[ ] **GUARD-P2-C** — julian_all.json → GH Release-only
    - cross-link to JULIAN session (data layout decision)
    - reduces repo bloat (commit cycle 6h × 15-30MB)

[ ] **T-09** — BIBLE dev tool LLM key inventory
    - add to `docs/SECRETS.md` § "Developer tool storage" (Groq/Gemini ใน localStorage)
    - cross-link to BIBLE session — owner BIBLE update SECRETS doc next session
    - link rotation to quarterly SOP-05 workflow

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED] — รอ user

ไม่เปลี่ยนจาก v2:
[ ] [ทดลองใช้] รัน Lighthouse mobile + desktop บน horatad.com → save to `docs/cia/lighthouse_*` (R-08)
[ ] [ทดลองใช้] GitHub Settings: 2FA + branch protection main + Dependabot + secret scanning (R-04 + R-10)
[ ] [ทดลองใช้] Cloudflare dashboard: Rate Limiting 30 req/min/IP (R-02 / T-02)
[ ] [ทดลองใช้] Export `horatad-auth` + `horatad-ai` Worker source → `workers/` → GUARD re-audit T-06
[ ] [ทดลองใช้] หลัง CSP Report-Only ทำงาน 1 wk → ส่ง browser console violations กลับมา
[ ] [BLOCKED] R-07 decision — Option A/B/C (GUARD recommends C, P1-E implements once confirmed)

---

## RISK REGISTER snapshot (post cross-project scan)

| ID | Status delta this session |
|---|---|
| R-19 (new) | **informational** — added; no Phase 1 action; HORATAD privacy disclosure cross-link suggested for future |
| B-01 / B-02 (verified safe) | **closed by audit** — BIBLE DSL + prompt construction both inspected and confirmed clean |
| **B-03 / T-09 (new)** | BIBLE dev tool keys in localStorage — **accept** w/ rotation cycle (SOP-05); audit XSS surface ของ tools/* ก่อน CSP enforce |

Open count unchanged: 5 P0/P1 blocked on user (Lighthouse, GH Settings, CF dashboard, Worker source, R-07 decision)

---

## WHY LOG (v3 additions)

- **เลือกทำ P2-D + cross-project scan ก่อน P2-A** — Phase 2 P2-A ต้อง prereq (CSP report-only 1 wk data). ขณะรอ user ทดสอบ + collect violations, autonomous docs work ฟรี + ปูทางการตัดสินใจ Option D vs E ให้ user เห็นชัด

- **R-19 เป็น informational ไม่ใช่ P-priority** — NOK current Phase 1 ใช้ local voice (lang=th-TH bias). cloud TTS อยู่ Phase 3 deferred. ไม่ต้องแก้ตอนนี้ แต่ track ไว้เพื่อให้ session ถัดไปไม่ลืมเมื่อ Phase 3 มา

- **BIBLE audit confirmed safe ไม่ใช่ "no finding"** — pre-audit assume safe ไม่ใช่หลักการ. การ inspect แล้ว verify มี value เพราะ:
  1. ทำให้ future Phase 2+ (free-text query) เริ่มจาก known clean baseline
  2. กันการ regress (ถ้าใครเปลี่ยน DSL ในอนาคต รู้ว่าต้อง re-audit)
  3. ใส่ใน docs ทำให้ session/project อื่นไม่ต้อง re-audit ซ้ำ

- **Option E (refactor) ดีกว่า Option D (status quo) สำหรับ Phase 2** — เพราะ:
  1. `'unsafe-inline'` defeats หลักของ CSP (anti-XSS-injection)
  2. Effort bounded (4-6 hr, ไม่มี dependency)
  3. Test surface manageable (manual click through ~30 min)
  4. Reversible (single commit revert)
  5. Permanent benefit (set-and-forget after refactor)
  Option D ทำให้ CSP เป็น "ป้ายสวยติดประตู ไม่ล็อค" — มี header แต่ป้องกัน XSS ไม่ได้จริง

- **Option F (nonce-based) ไม่ viable** — GitHub Pages serves static files; ไม่ inject per-request nonce ได้. ต้องย้ายไป CF Pages with Functions ซึ่ง scope ใหญ่กว่า Phase 2 ทั้งหมด → ตัดทิ้ง

- **เลื่อน R-07 / P1-E implementation จริงๆ** — แม้ session BIG อาจอยาก close loop, แต่ Option C เปลี่ยน user-facing UX (modal เตือนก่อน share). ทำได้แค่ confirm จาก user. GUARD แนะนำ ≠ user agree

---

## Cross-link checklist (updated v3)

- [x] `docs/GUARD_MISSION.md` — R-19 added § 3.2
- [x] `docs/cia/csp_policy.md` — Phase 2 P2-A plan ready
- [x] `docs/cia/cross_project_scan_2026-05-23.md` — NOK/BIBLE/REORG/JULIAN delta
- [ ] `handoffs/NOK_*` — note R-19 informational + Phase 3 cloud TTS audit requirement (defer to NOK session)
- [ ] `handoffs/HORATAD_*` — future: TTS privacy disclosure when Phase 3 lands (defer)

---

## Next session command

User พิมพ์: **`session GUARD`** (Phase 2 ready) — เมื่อ:
1. User รัน Lighthouse + commit reports → ทำ R-08 (perf measure follow-up)
2. CSP report-only week + browser console violations → execute P2-A Option E refactor
3. User export Worker source → re-audit T-06
4. User ตัดสินใจ R-07 Option C → ทำ P1-E (~45 min)

ระหว่าง wait — autonomous backlog:
- ตอนนี้ docs ครบสำหรับ Phase 2 ไม่มี pure-docs งานเพิ่มที่ทำได้แล้ว
- ถ้า user อยาก speed up: P2-A สามารถเริ่ม refactor ก่อนได้โดย hypothesis "report-only จะแสดง 196 handler violations + 1 inline style violation" (ตามที่ inventory บอก) — ความเสี่ยงต่ำเพราะ inventory แม่นยำ

---

## 📨 Incoming from JULIAN (2026-05-24)

**JULIAN Phase 1-2 deployed: raw source bucket architecture** — ต้อง workflow update เพื่อ activate ใน production

### สิ่งที่ JULIAN ทำไปแล้ว
- `workers/julian_raw_writer.mjs` — append-only writer สำหรับ raw buckets
- `data/julian_raw/<source>.jsonl` — โครงสร้าง bucket แยกตาม source
- Scrapers ทั้ง 3 (wikidata/astrotheme/wiki_th) ทำ dual-write แล้ว
- `workers/julian_merge.mjs` — standalone merge raw → julian_all.json (priority: seed > wiki_th > astrotheme > wikidata > existing)

### ⚠️ GUARD ต้องทำเพื่อให้ activate
**`.github/workflows/julian_sync.yml` ต้อง commit ไฟล์ใน `data/julian_raw/` ด้วย** — ตอนนี้ workflow add เฉพาะ `data/julian_all.json` → raw จะหายไปกับ GHA ephemeral runner

**แนะนำ patch:** เพิ่ม `git add data/julian_raw/*.jsonl` ในทุก step ที่มี scrape:
1. Commit progress step (หลัง Wikidata scrape) — เพิ่ม `git add data/julian_raw/wikidata.jsonl`
2. Astrotheme enrichment — เพิ่ม `git add data/julian_raw/astrotheme.jsonl`
3. Wikipedia TH — เพิ่ม `git add data/julian_raw/wikipedia_th.jsonl`

### Optional ต่อจากนี้ (Phase 4 ของ JULIAN architecture)
แทน inline-JS merge ใน workflow ด้วย `node workers/julian_merge.mjs` — clean up workflow yaml + ใช้ logic ที่ test แล้วใน standalone script

ทดสอบความเทียบเท่ากัน: end-to-end test ใน JULIAN_20260524 ผ่าน — merged 60,180 + 2 raw → 60,182 (with Astrotheme enrich + accuracy bump D→C ทำงานถูก)

### Risk assessment (JULIAN-self)
- ไม่กระทบ HORATAD (julian_all.json schema เหมือนเดิม)
- ไม่กระทบ workflow ปัจจุบัน (dual-write — ไม่ลบ path เดิม)
- raw bucket size estimate: ~60MB total เมื่อเต็ม 100K records × 3 sources × ~200 bytes
