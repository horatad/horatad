# JULIAN — Session Handoff
# Date: 2026-05-21 (session 4)
# Previous: handoffs/JULIAN_20260521_v3.md

## STATE
App version : N/A (JULIAN ไม่มี version bump — tools + workers)
DB storage  : ✅ CF D1 online — 400 records ใน internet table
Records     : 400/500 (นักการเมืองไทย batch 1+2 — 2026-05-21)
Automation  : ✅ GitHub Actions cron — daily scrape + weekly empirical ทำงานปกติ
Status tool : ✅ tools/julian_status.html — แสดงข้อมูลจาก report.json แล้ว

---

## DONE (session นี้)

✓ Root cause ของ auth error 10000/9109 (Runs #8-11) ระบุได้แล้ว:
  - สร้าง CF API token ใหม่ใน CF Dashboard ✅
  - **ลืม update GitHub Secret CF_API_TOKEN** → token เก่า (หรือผิด) อยู่ใน Secret → auth fail
  - user ใส่ token ใหม่ใน Secret → Run #12 ผ่าน ✅

✓ tools/julian_status.html สร้างแล้ว — อ่านจาก static JSON ใน repo (ไม่มี CORS)
  URL: https://horatad.github.io/horatad/tools/julian_status.html
  แสดง: total records / rate / ETA / categories / รายชื่อล่าสุด 100 คน

✓ workflow improvement (Run #12+):
  - pin wrangler@4 (ไม่ดึง major version ใหม่โดยไม่ตั้งใจ)
  - เพิ่ม curl token verify ก่อน wrangler — ถ้า auth fail ครั้งต่อไปจะเห็นทันทีใน log

✓ julian_report.json ถูก generate แล้ว (Run #12):
  - total: 400 | by_category: นักการเมืองไทย TH (400) | recent: 100 คนล่าสุด

---

## Root Cause Log (สำหรับ debug ครั้งต่อไป)

**⚠️ Pattern: auth fail หลัง roll token**
เมื่อ CF API token ถูก roll (regenerate) ใน CF Dashboard → ต้อง **update GitHub Secret CF_API_TOKEN** ด้วย
ถ้าลืม → workflow รัน error 10000 (Authentication error) + 9109 (Invalid access token)
token verify step ใน workflow (curl) จะแสดง `token status: active` หรือ `disabled` ก่อน wrangler รัน

---

## PENDING — 🔴 รอ user

[ ] [ทดลองใช้] ดู julian_status.html — ตรวจว่าแสดงข้อมูล 400 records ถูกต้อง
    URL: https://horatad.github.io/horatad/tools/julian_status.html

[ ] [BLOCKED] master_key import — ยังไม่มีข้อมูล (tables exist แต่ master_key ว่าง):
    node workers/julian_keygen.mjs 1700 2100 > julian_mk_1700-2100.csv
    node workers/julian_import.mjs julian_mk_1700-2100.csv > import_mk.sql
    wrangler d1 execute julian --file=import_mk.sql --remote --yes

[ ] [BLOCKED] deploy Worker: cd workers && wrangler deploy --config julian_wrangler.toml
    (ต้องทำก่อน BIBLE ↔ JULIAN API จะใช้ได้)

[ ] [BLOCKED] policy: HUMAN record ที่ไม่มี relate_id — ใช้ status='unvalidated' flag?
    (ค้างจาก session v2)

---

## DEFERRED

[ ] M7 Data Quality — validated/unvalidated policy
[ ] relate_id schema — JD array หรือ {type, jd} object?
[ ] BIBLE ↔ JULIAN query API spec — รอ CF D1 + data
[ ] Rating UI (👍/👎) — รอ HORATAD + BIBLE เสถียร

---

## Context สำคัญ

**D1 database_id:** eb644547-f275-41b9-9bb3-204fb1b28eb7 (ตั้งใน wrangler.toml แล้ว)

**Automation schedule:**
- Daily 02:00 UTC: Wikidata scrape → D1 import → generate report.json
- Weekly Sunday 03:00 UTC: empirical validation → update kb.json
- หยุดเองเมื่อ 500 records (workflow disable อัตโนมัติ)
- 400/500 แล้ว — เหลืออีก ~2 run (100 records ต่อ run)

**⚠️ เวลา roll CF API token:**
1. CF Dashboard → My Profile → API Tokens → roll
2. **ต้อง update GitHub Secret CF_API_TOKEN ด้วยทันที**
   GitHub repo → Settings → Secrets → CF_API_TOKEN → update
