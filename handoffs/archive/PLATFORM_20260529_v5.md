# PLATFORM — Session Handoff
# Date: 2026-05-29 (session 5 ของวัน)
# Previous: handoffs/PLATFORM_20260529_v4.md

## STATE
Branch  : claude/cool-gauss-OSOH3 → ff main ✅ (043d41c)
Pipeline: inbox → scheduled → posted · autopost ทำงานอัตโนมัติ 08:30 ICT แล้ว

## DONE (session นี้ — ต่อจาก v4)

✓ **Concern 1+2 (policy gate shared module)** — `workers/fb_policy.mjs` ใหม่ (6 rules)
  - content_curator.mjs + content_requeue.mjs import gate ร่วมกัน
  - requeue กรอง rawPool ก่อนเข้าคิว + ย้าย rejected กลับ inbox

✓ **Concern 7 (CRITICAL — queue stall)** — ยกเว้น `education` + `behind_scenes` จาก formatRepeat
  - ก่อนแก้: 95% inbox = education → หลัง 3 วัน rule block ทุกอย่าง → คิวหยุด
  - `VARIATION_EXEMPT_CATS = new Set(['education', 'behind_scenes'])`
  - workers/fb_policy.mjs บรรทัด ~44

✓ **Concern 8 (silent failure)** — fb_autopost.yml สร้าง GitHub Issue ถ้า workflow fail
  - step "Notify on failure" ใช้ `if: failure()` + `gh issue create`
  - label "alert" (fallback: ถ้าไม่มี label ก็ create ได้อยู่)

## FB Policy Gate — 6 กฎ (workers/fb_policy.mjs)
| # | กฎ | ยกเว้น |
|---|---|---|
| 1 | Dead content (video ถูกลบ) | ไม่มี |
| 2 | Engagement bait | ไม่มี |
| 3 | ต้องมี disclaimer | finding/case_study เท่านั้น |
| 4 | Accuracy claim ต้องมี n= | finding/case_study เท่านั้น |
| 5 | Format variation (category ซ้ำ 3 วัน) | **education, behind_scenes ยกเว้น** |
| 6 | Prediction framing | finding/case_study เท่านั้น |

## Content Pipeline — สถานะปัจจุบัน
```
YouTube → youtube_sync → content/inbox/
                ↓ content_curator (08:00 ICT, cron)
         content/scheduled/   ← fb_policy gate 6 rules
                ↓ fb_autopost (08:30 ICT, cron)
         Facebook Page
                ↓ commit
         content/posted/
```
**KPI**: fb_kpi_monthly.yml → `content/kpi/YYYY-MM.json` (1st ของทุกเดือน)

## Token — สถานะ (verified 2026-05-29)
| | |
|---|---|
| Type | Page token ✅ |
| Expires | Never ✅ |
| Scopes | pages_show_list · pages_read_engagement · pages_manage_posts · public_profile |
| Data Access | ~ปลาย ส.ค. 2026 (90 วัน GDPR) — posting ไม่กระทบ แต่ engagement read อาจ fail |
| read_insights | ยังไม่เพิ่ม — รอ ≥100 followers |

## PENDING — 🟢 Claude ทำเองได้
[ ] ตรวจ `alert` label มีใน repo ไหม — ถ้าไม่มี concern 8 ยัง fallback ได้ แต่ควรสร้าง label ให้ชัด
    `gh label create "alert" --color "e11d48" --description "Auto-post failure alert"`

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]
[ ] [ทดลองใช้] ตรวจ 08:30 ICT วันถัดไปว่า EP ขึ้น Page จริงไหม (ยืนยัน pipeline end-to-end)
[ ] [ทดลองใช้] fb_post_helper → https://horatad.com/tools/fb_post_helper.html — verify backlog section แสดงถูก
[ ] [ทดลองใช้] ทดสอบ concern 8: trigger workflow fail ด้วยมือ แล้วตรวจว่า issue ถูกสร้าง

## DEFERRED
[ ] `/published_posts` vs `/posts` — posts=0 issue ใน KPI (low priority — page มีแค่ 5 followers)
[ ] `read_insights` scope — เพิ่มตอน regenerate token เมื่อ followers ≥100
[ ] Token Data Access re-auth — ปลาย ส.ค. 2026 (ถ้า KPI report fail ที่ engagement)

## WHY LOG
- **Concern 7 exempt education (2026-05-29)** — inbox 95% = YouTube EP (category=education) · ถ้าไม่ยกเว้น variation rule block ทุกอย่างหลัง 3 วัน → pipeline หยุดเอง · education ไม่ใช่ spam signal เพราะเนื้อหาต่างกันทุก EP · variation rule ไว้จับ research/finding ที่ซ้ำ category จริงๆ
- **Concern 8 GitHub Issue (2026-05-29)** — ไม่มี notification = Peter ไม่รู้ว่าโพสต์พัง · GitHub Issue ฟรี + ส่ง email notification อัตโนมัติ · ใช้ `gh issue create` ใน workflow (GITHUB_TOKEN มีอยู่แล้ว) · `2>/dev/null || fallback` กัน fail ถ้า label "alert" ไม่มี
- **Shared policy gate (2026-05-29)** — curator + requeue เดิมมี gate แยกกัน → drift risk · refactor เป็น fb_policy.mjs เดียว = fix ที่เดียว apply ทุกที่ · requeue ยังกรอง rawPool ด้วย gate (concern 2) ไม่ให้ content เสียข้ามผ่านตอน rebuild queue
