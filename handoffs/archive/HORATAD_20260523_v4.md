# HORATAD — Session Handoff
# Date: 2026-05-23 (session 4 ของวัน)
# Previous: handoffs/archive/HORATAD_20260523_v3.md

## STATE
App version : V3.3.23 (main + deployed) — ไม่ bump (เป็น doc/meta session)
Backups     : backup/v3.3.16–v3.3.22-main (V3.3.23 ยังไม่ backup branch — รอ test mobile)

## DONE (session นี้ 2026-05-23 v4)
✓ **Trim CLAUDE.md** (BIG-owned deferred task — เสร็จที่นี่)
  - HTML Tools section: ลบ duplicate `App หลัก ทดสอบที่ horatad.com` (มีใน docs/HORATAD.md อยู่แล้ว) → แทนด้วย generic "ดู project doc"
  - Deploy strategy: "6 ไฟล์" → "หลายไฟล์ (เช่น HORATAD 6 ไฟล์ ดู docs/HORATAD.md)" — เป็นกลางสำหรับทุก project
  - Token efficiency #1: `script.js` example → generic file placeholder + HORATAD reference
  - Session limit signal: stale "script.js 330KB" → "เช่น HORATAD `script.js` 200KB+" (sync กับ V3.3.23 size)
✓ **เพิ่ม Session Tips ใน docs/HORATAD.md** — capture context ที่ generalize ออกจาก CLAUDE.md:
  - script.js size + grep-first pattern
  - Version bump 6 files overhead → batching importance
  - KB async load + guard pattern
  - Production URL = horatad.com
✓ **อัปเดต PROJECT_STATUS.md** — note `Trim CLAUDE.md` ✅ done ใน REORG closure section (BIG should mark complete in next BIG session)
✓ Rebase + push (origin/main ขยับเพราะ BIBLE session คั่น) — `git push --force-with-lease` ที่ feature branch + ff main สำเร็จ

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

⚠️ **HORATAD-scope autonomous work เหลือน้อย** — Step 1+ (code split) ขึ้นต่อ CSP enforce (P2-A) ที่ blocked on user

[ ] **Phase 2 Step 1 — core/ Tier 1 split** — ⚠️ ไม่แนะนำทำตอนนี้
    - เหตุผล: ขึ้นต่อ CSP enforce (per `docs/HORATAD_modules.md` §8) — ถ้า ES module ตอนนี้ + CSP enforce ภายหลัง ต้องแก้ inline event handlers 2 รอบ
    - แนะนำ: รอ Lighthouse re-run (วัดผล Step 0) + CSP observation 1 wk ก่อนตัดสิน

[ ] **Phase 2 Step 2 — db/ Tier 2 split** — same constraint as Step 1

[ ] **PROVINCES extract** — DEFERRED — ใช้ใน hot calc path

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ทดสอบ V3.3.23 บนมือถือ — KB async load + ทำนาย flow + race UX toast
[ ] [ทดลองใช้] ทดสอบ V3.3.22 บนมือถือ — M6 persist + nav "จร" compareMode===3
[ ] [ทดลองใช้] ทดสอบ V3.3.21 บนมือถือ — T1/T2 + transit unit popup + eventChart cycling
[ ] [ทดลองใช้] ทดสอบ NOK TTS guide บน Android Chrome
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [ทดลองใช้] Lighthouse mobile + desktop → `docs/cia/lighthouse_*` (วัดผล V3.3.23 -49%)
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy Option A/B/C → GUARD R-07
[ ] [BLOCKED] julian_all.json move target — รอ user เลือก
[ ] [BLOCKED] CSP enforce (P2-A) — รอ user ส่ง browser violations 1 wk Report-Only

## 📨 Outgoing to BIG
- "Trim CLAUDE.md" task (BIG handoff `handoffs/BIG_20260523_v1.md` line 59) — ✅ done by HORATAD v4 session. BIG ควร mark complete ใน BIG handoff รอบถัดไป

## WHY LOG
- **เลือกข้อ 1 (Trim CLAUDE.md) แทนข้อ 2-3** — payback ชัด ทุก session start ประหยัด token เล็กน้อย + ลด confusion ตอน BIBLE/JULIAN/NOK session อ่าน CLAUDE.md แล้วเจอ HORATAD-specific detail
- **Generalize แทน ลบ** — HORATAD-specific examples ใน CLAUDE.md ส่วนใหญ่เป็น "concrete example" ของ universal principle (เช่น "grep ก่อน read") → ลบหมดจะเสีย context ที่ดี → เปลี่ยนเป็น generic + cite HORATAD as example เก็บ value ไว้
- **ไม่บับ version** — session นี้แตะแค่ doc files (CLAUDE.md, docs/HORATAD.md, PROJECT_STATUS.md, handoffs) — ไม่กระทบ app code → ไม่ต้อง version bump + cache bust
- **Force push feature branch หลัง rebase OK** — rule "ห้าม force push ไป main" ยังคงอยู่ feature branch (claude/horatad-2zwk8) เป็นของ session นี้ เพิ่งสร้าง ไม่มีใคร depend on SHA เก่า → `--force-with-lease` ปลอดภัย
- **REORG ถูก closed กลางทาง (โดย BIG session ก่อนหน้า) ทำให้เกิด conflict** — main ขยับระหว่างที่ HORATAD session ทำงาน → ต้อง rebase + แก้ conflict โดย accept HEAD (REORG closure structure) + note done-ness ของ task ที่โอนไป BIG
