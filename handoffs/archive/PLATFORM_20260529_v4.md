# PLATFORM — Session Handoff
# Date: 2026-05-29 (session 5 ของวัน)
# Previous: handoffs/archive/PLATFORM_20260529_v3.md

## STATE

| | |
|---|---|
| Phase | **🟢 Phase 3 LIVE** — FB Auto-post ทำงานจริง EP01 โพสต์แล้ว |
| FB Page | โหราทาส AI (`FB_PAGE_ID=1095745230295693`) |
| Token | `FB_PAGE_TOKEN` + `FB_PAGE_ID` ตั้งใน GitHub Secrets แล้ว (Page token, expires ~60 วัน) |
| Auto-post | cron ทุกวัน 08:30 ICT (`30 1 * * *`) — อัตโนมัติเต็มระบบ |
| content/scheduled | **13 โพสต์** (EP01 โพสต์ออกแล้ว เหลือ 13) |
| content/inbox | 95 (backlog: รวม series เกิน cap 6 ตัว) |
| content/posted | 2 (EP05 + EP01) |
| App ver | (PLATFORM ไม่มี version — ใช้ workflow/script) |

---

## DONE (session 5 นี้)

✓ **fix hook shallow-clone false-alarm** (`ec52401`) — `.claude/hooks/session-start.sh`
   - root cause: container clone `--depth 50` → ไม่เห็น common ancestor → นับ history ที่ถูกตัด = 50 → "commits ค้าง 50" ทั้งที่ synced จริง
   - fix: shallow-aware guard 3 ชั้น (exact-match / shallow+no-merge-base ข้ามนับ / full clone นับปกติ)
   - ⚠ note BIG handoff: "3 BIBLE branches drift" น่าจะ shallow-distorted เหมือนกัน — ต้อง re-verify
✓ **requeue: series ≤2/สัปดาห์** (`ff79abc` logic + `439875c` run จริง) — `workers/content_requeue.mjs`
   - เดิม round-robin เท่ากัน → series ~67% · ใหม่ greedy ratio cap 2/7 (29%) ตำแหน่ง 4,7,11,14
   - ดึง EP จาก inbox เติม · series เกิน cap ย้ายกลับ inbox (เดิมโดนลบหาย — fixed)
   - `--days=N` (default 14) · ลบ dead code groupKey/GROUP_PRIORITY
✓ **⭐⭐ Setup FB Page Token + GitHub Secrets** — Phase 3 ปลดล็อก (ปีเตอร์ทำเอง browser-only ตาม Secret Handling Rule)
   - flow: short token → long-lived (fb_exchange_token) → Page token (me/accounts data[0].access_token) → Secrets
   - gotcha: data[0].access_token = Page token (≠ token ที่ใช้ call) · client_secret error = App Secret copy ผิด/ไม่ครบ
✓ **fb_autopost workflow run สำเร็จ** — EP01 โพสต์ขึ้น Page จริง · bot commit ย้าย scheduled→posted อัตโนมัติ (`ba69982`)
✓ **bump GHA actions v4→v5** (`5026ae3`) — checkout/setup-node ทั้ง 3 workflow (fb_autopost/curator/youtube_sync)
   - แก้ Node 20 deprecation (GitHub บังคับ Node 24 มิ.ย. 2026) · dry_run ย้ายไป env DRY_RUN (robust)

---

## PENDING — 🟢 Claude ทำเองได้

[ ] **research post ชุดต่อไป** — FAQ / roadmap / accuracy grade เชิงลึก จาก julian_research_report.pdf
    · path: `workers/julian_research_gen.mjs` → buildResearchCaption() → checkResearchPolicy() → inbox
[ ] **refill คิวรอบหน้า** — เมื่อ scheduled ใกล้หมด รัน `node workers/content_requeue.mjs` ดึง backlog inbox มาเติม (series ที่เหลือ 6 ตัว + EP)
[ ] **Batch curator --count N** — ถ้าต้องการเร่งคิว

## PENDING — 🔴 [ทดลองใช้]

[ ] **[ทดลองใช้] ทดสอบ fb_post_helper บนมือถือ** — manual posting flow (copy + push button)
[ ] **[ทดลองใช้] verify cron โพสต์อัตโนมัติพรุ่งนี้ 08:30** — EP02 ควรขึ้นเอง (ถ้าไม่ขึ้น ตรวจ Actions log)
[ ] **[ทดลองใช้] FB_PAGE_TOKEN หมดอายุ ~60 วัน** — ราว ปลาย ก.ค. 2026 ต้อง refresh (Page token จาก long-lived ปกติไม่หมด แต่ verify ใน debug tool)

## DEFERRED
[ ] LINE OA chatbot · Content auto-generation

---

## 📨 Incoming → GUARD (2026-05-29)
- workflow อื่นที่ยังใช้ `checkout@v4`/`setup-node@v4` (ไม่ใช่ PLATFORM 3 ตัว) ควร bump เป็น v5 ด้วย — version policy (Node 20 deprecation มิ.ย. 2026)
- ตรวจ: `grep -l "checkout@v4\|setup-node@v4" .github/workflows/*.yml`

## 📨 → BIG (ดู BIG_20260529_v1.md section Incoming from PLATFORM)
- hook shallow-aware fix + ขอ re-verify "3 BIBLE branches drift" (น่าจะ shallow-distorted)
- durable fix: ตั้ง web env setup script ให้ `git fetch --unshallow origin`

---

## WHY LOG (session 5)

- **Page token แยกจาก user token (2026-05-29)** — `me/accounts` คืน `data[0].access_token` = Page token (ไม่หมดอายุ/อายุยาว) คนละตัวกับ user token ที่ใช้ call · ใช้ user token โพสต์ Page → error #200 "requires pages_manage_posts as admin"
- **dry_run ไม่ใช่ bug (2026-05-29)** — logic ถูก · รอบแรกปีเตอร์ปล่อย input default (false) → โพสต์จริง · ปรับเป็น env DRY_RUN กัน injection + อ่านง่าย
- **Node deprecation อยู่ที่ action ไม่ใช่ script (2026-05-29)** — `node-version:'22'` คุม script เรา · warning มาจาก action wrapper (checkout/setup-node@v4 รันบน Node 20 runtime) → fix = bump action major เป็น v5
- **shallow clone หลอก git diff (2026-05-29)** — `--depth 50` ทำให้ merge-base หาย → ทุก command ที่พึ่ง ancestor (log A..B, branch drift count) เพี้ยน · เจอตัวเลข = depth พอดี = สัญญาณ shallow artifact
- **requeue cap แบบ greedy ratio (2026-05-29)** — `(series+1)/(total+1) <= 2/7` ให้ series ตกตำแหน่ง 4,7 ของสัปดาห์เป๊ะ · ดีกว่า weekly-window fix เพราะ degrade graceful เมื่อ series หมด (สลับเป็น EP ล้วนเอง)
