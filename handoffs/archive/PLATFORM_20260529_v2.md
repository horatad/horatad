# PLATFORM — Session Handoff
# Date: 2026-05-29 (session 2 ของวัน)
# Previous: handoffs/PLATFORM_20260529_v1.md

## STATE

| | |
|---|---|
| Phase | Phase 3 auto-post **BUILT** — รอปีเตอร์ตั้ง Page token secret (ไม่ต้อง App Review!) |
| FB Page | โหราทาส AI |
| content/inbox | 104 ไฟล์ รอ curate (99 เดิม + 5 TALS comparison series ใหม่) |
| content/scheduled | 5 (EP01–EP04, EP06 — curator populate แล้ว) |
| content/posted | 1 โพสต์ — EP05 ดาวบาปเคราะห์/ศุภเคราะห์ (2026-05-28) |

---

## DONE (session 3 — auto-post pipeline)

✓ **Phase 3 auto-post — BUILT** (เดิม BLOCKED เพราะเข้าใจผิด → ปลดล็อกด้วย Standard Access)
   - `workers/fb_autopost.mjs` — โพสต์คิวถัดไป (FIFO ตาม scheduled_at) ขึ้น FB Page ผ่าน
     Graph API → ย้าย scheduled/ → posted/ · weekly cap 14 · bait gate · `--dry-run`/`--force`
   - `workers/fb_token_exchange.mjs` — แลก short → long-lived → never-expiring Page token
   - `.github/workflows/fb_autopost.yml` — cron 08:30 ICT (หลัง curator) · skip เงียบถ้าไม่มี secret
   - ✅ dry-run ทดสอบผ่าน (เลือก EP01 ถูก, link preview ใช้ youtube_url, weekly count ทำงาน)
   - Backup: `backup/platform-autopost-20260529`
   - ⭐ **รอปีเตอร์ 1 step:** generate Page token → ตั้ง GitHub Secret (ดู PENDING 🔴)

✓ **สร้าง Meta Developer account + App "โหราทาส AI"** (session นี้ ปีเตอร์ทำผ่าน UI)
   - Use case: Manage everything on your Page (มี pages_manage_posts)
   - Business Portfolio: ยังไม่ผูก (ไม่จำเป็นสำหรับ Standard Access)
   - Ad account identity verification: ✅ ผ่าน 2 รายการ (payer/beneficiary)

## DONE (session 1-2)

✓ **privacy.html** — สร้างแล้ว deploy บน `horatad.com/privacy.html`
   - ข้อมูล: ไม่เก็บ personal data, contact@horatad.com, last updated 2026-05-29

✓ **TALS Comparison Series — 5 FB posts** เข้า inbox แล้ว
   - Post 01: Hook — โหราศาสตร์ที่พิสูจน์ได้ มีจริงหรือ?
   - Post 02: Core concept — ภพเรือนบอกเรื่องราว ดาวเคราะห์บอกคุณภาพ
   - Post 03: 5 Axioms ของ TALS
   - Post 04: TALS vs Western vs Vedic vs ไทยดั้งเดิม
   - Post 05: คุณภาพดาว 11 ระดับ + กฎ ลบ+ลบ=บวก
   - priority_score=9 (สูงกว่า EP series ที่ 8) → curator จะเลือกก่อน
   - source_doc: docs/tals_comparison/tals_comparison.md

---

## ✅ META APP REVIEW — ไม่ต้องทำแล้ว! (2026-05-29 พลิก)

**สรุปการตัดสินใจ:** ปีเตอร์โพสต์ลง Page ตัวเอง คนเดียว → ใช้ **Standard Access** พอ
→ **ไม่ต้อง** App Review / Business Verification / Tech Provider / screencast / follower

ความเข้าใจผิด 2 ชั้นที่ debunk วันนี้:
- ❌ "ต้องมี follower minimum" — Meta App Review ไม่เคยมีเงื่อนไขนี้ (เดาผิดมาแต่ session เก่า)
- ❌ "ต้อง App Review" — จริงเฉพาะตอน **คนอื่น** ใช้ app โพสต์ลง Page **ของเขา**

App Review จะต้องทำก็ต่อเมื่ออนาคตเปิดให้คนนอกใช้ — ตอนนี้ข้ามทั้งหมด

## ⭐ ขั้นตอนเดียวที่ปีเตอร์ต้องทำ — Setup Page Token

### ⚠️ บทเรียน session 3 — Graph API Explorer ตันสำหรับ app นี้

ลองผ่าน Explorer แล้ว **ติด** — สรุปสิ่งที่เจอ (อย่าเสียเวลาซ้ำ):
- App ใช้ product **"Facebook Login for Business"** → ต้องสร้าง **Configuration** ก่อน Explorer
  ถึงจะ gen User Token ได้ (error "no configuration available")
- สร้าง config **"autopost"** แล้ว (ID: `1321339116801207`, access token type = User, perm: `pages_show_list`)
  **แต่** config นี้ไม่โผล่ใน dropdown ของ Explorer (คนละระบบ)
- Explorer dropdown "Add a Permission" มีให้แค่ **`business_management` + `pages_show_list`**
  → **`pages_manage_posts` / `pages_read_engagement` ไม่อยู่ในลิสต์** (Standard Access จำกัด)
- System-user token ก็ล็อก (app ยังไม่ผูก Business Portfolio)

### ✅ ทางที่แนะนำ session หน้า — OAuth URL ตรง (ข้าม Explorer)

ปีเตอร์ส่ง **App ID + App Secret** (จาก App → Settings → Basic, กด Show) ให้ Claude →
Claude สร้าง OAuth dialog URL พร้อม scope `pages_manage_posts,pages_read_engagement,pages_show_list`
→ ปีเตอร์เปิดในบราวเซอร์ login → ได้ `code` → แลกเป็น user token → `fb_token_exchange.mjs` ต่อ

OAuth URL pattern:
```
https://www.facebook.com/v23.0/dialog/oauth?client_id=<APP_ID>
  &redirect_uri=https://horatad.com/privacy.html
  &scope=pages_manage_posts,pages_read_engagement,pages_show_list
  &response_type=code
```
(redirect_uri ต้องเพิ่มใน App → FB Login → Valid OAuth Redirect URIs ก่อน · หรือใช้
 `https://www.facebook.com/connect/login_success.html` แบบ token flow `response_type=token`)

**ทางเลือกสำรอง (robust กว่า):** ผูก app เข้า Business Portfolio → ปลดล็อก System-user token
(ดีสุดสำหรับ cron — ไม่ผูก account ส่วนตัว ไม่หมดอายุ) แต่ต้อง setup portfolio ก่อน

### หลังได้ token แล้ว (เหมือนเดิม)

1. **แลก Page token** (Claude รันให้ใน sandbox):
   ```
   FB_APP_ID=.. FB_APP_SECRET=.. FB_SHORT_TOKEN=.. node workers/fb_token_exchange.mjs
   ```
   → ได้ `FB_PAGE_ID` + `FB_PAGE_TOKEN`

2. **ตั้ง GitHub Secrets** → repo Settings → Secrets → Actions: `FB_PAGE_ID`, `FB_PAGE_TOKEN`

3. **ทดสอบ** → Actions → "FB Auto-post" → Run workflow → dry_run=true → แล้วปล่อย cron 08:30 ICT

---

## PENDING — 🟢 Claude ทำเองได้

[ ] **content inbox curation** — 104 ไฟล์รอ · curator รัน 1/วัน อัตโนมัติ
    · TALS comparison series (priority 9) จะถูกเลือกก่อน EP series (priority 8)
    · ถ้าต้องการ curate batch → `node workers/content_curator.mjs --target=10`

[ ] **Batch curator --count N** — ปัจจุบัน top 5 buffer · ถ้าต้องการเร่ง
    · เพิ่ม flag `--count N` ใน content_curator.mjs → schedule N วันพร้อมกัน

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] **[ทดลองใช้] ⭐ setup PAT ใน fb_post_helper** (prerequisite ก่อน mark posted จริง)
    · เปิด `https://horatad.com/tools/fb_post_helper.html`
    · กาง 🔑 → วาง Classic PAT (scope `public_repo`) → บันทึก
    · ทดสอบ: กด "โพสต์แล้ว" → ไฟล์ควรย้ายออกจากคิว

[ ] **[ทดลองใช้] ⭐ ทดสอบ fb_post_helper UI บนมือถือ**

[ ] **[ทดลองใช้] ⭐⭐ Setup Page Token + GitHub Secrets** — เปิด Phase 3 auto-post (ดูขั้นตอนด้านบน)
    · code พร้อมแล้ว (fb_autopost.mjs + workflow) เหลือแค่ token
    · workflow จะ skip เงียบจนกว่าจะตั้ง FB_PAGE_TOKEN secret

~~[BLOCKED] App Review / Business Verification / Screencast~~ — **ยกเลิก** (ใช้ Standard Access ไม่ต้องทำ)

---

## DEFERRED

[ ] **LINE OA chatbot** — รอ HORATAD + BIBLE พร้อม + LINE OA setup
[ ] **Content auto-generation** — รอ BIBLE generate script สมบูรณ์

---

## PIPELINE ARCHITECTURE (Phase 1 — ตอนนี้)

```
Google Drive _FB&Social
   ↓ Apps Script sync → content/inbox/  (104 ไฟล์)
   ↓ content_curator.yml (cron 08:00 ICT, buffer=5, bait gate ✅)
content/scheduled/  ─┬─→ fb_post_helper.html → ปีเตอร์โพสต์มือ (manual loop)
                     │                              ↓ กด "โพสต์แล้ว" (PAT)
                     └─→ fb_autopost.yml (cron 08:30 ICT) → Graph API → อัตโนมัติ
                                                    ↓
                                              content/posted/  (diversity feedback ✅)
```

**Phase 3 (auto):** fb_autopost.mjs ใช้ FB_PAGE_TOKEN (never-expiring) ยิง Graph API `/{page}/feed`
ทั้ง manual helper และ auto cron ใช้ token/คิวชุดเดียวกัน — เปิด-ปิด auto ได้โดยไม่กระทบ manual

**URL tools:**
- `https://horatad.com/tools/fb_post_helper.html` — คิวโพสต์ + mark posted
- `https://horatad.com/tools/index.html` — tools รวม
- `https://horatad.com/privacy.html` — Privacy Policy (Meta App Review) ✅ ใหม่

---

## WHY LOG

- **Phase 3 ไม่ต้อง App Review (2026-05-29 พลิก)** — ปีเตอร์โพสต์ Page ตัวเอง คนเดียว = Standard Access พอ. App Review/Business Verification/follower เป็นเงื่อนไขเฉพาะตอนเปิดให้คนนอกใช้ app โพสต์ลง Page ของเขา. เหมือนกุญแจบ้านตัวเอง — เปิดประตูบ้านตัวเองไม่ต้องขออนุญาตเทศบาล. ตัดงานหนักออกหมด เหลือแค่ generate Page token → ตั้ง secret
- **Graph API Explorer ตันกับ use-case app (2026-05-29 session3)** — app ที่สร้างด้วย use case "Manage everything on your Page" จะพ่วง "Facebook Login for Business" ซึ่ง (1) บังคับสร้าง Configuration ก่อน gen User Token (2) Explorer แสดง permission ได้แค่ `business_management`+`pages_show_list` — `pages_manage_posts` ไม่โผล่. ทางออก: ใช้ OAuth dialog URL ตรง (สร้างจาก App ID) หรือผูก Business Portfolio เพื่อปลดล็อก System-user token. **อย่าวนกลับไปงม Explorer**
- **fb_autopost link param (2026-05-29)** — ส่ง `link` (youtube_url) แยกจาก message เพื่อให้ FB เด้ง preview card. ถ้าใส่ URL ใน message เฉยๆ API ไม่ render preview ให้เสมอ
- **workflow skip-if-no-secret (2026-05-29)** — fb_autopost.yml เช็ค FB_PAGE_TOKEN ก่อน ถ้าไม่มี skip เงียบ ไม่ fail — กัน Actions แดงทุกวันระหว่างรอปีเตอร์ตั้ง token
- ~~manual loop ก่อน auto — Phase 3 ต้อง Meta App Review~~ (ผิด — ดูข้อบนสุด)
- **posted/ feedback loop (2026-05-29)** — curator diversity check ใช้ `posted/` กันโพสต์ category ซ้ำ 7 วัน
- **PAT optional แต่สำคัญ (2026-05-29)** — ไม่ใส่ PAT ก็ copy ได้ แต่คิวไม่ย้าย = posted/ ว่างซ้ำ
- **privacy.html เร่งด่วน (2026-05-29)** — Meta App Review ไม่ต้องรอ follower ปัญหาหลักคือ privacy URL + Business Verification → ทำได้เลย ปีเตอร์สั่งเร่ง
- **TALS series priority 9 (2026-05-29)** — สูงกว่า EP YouTube (8) เพราะ research content หายาก + เชื่อมกับ JULIAN empirical data → เหมาะสาย education/research
