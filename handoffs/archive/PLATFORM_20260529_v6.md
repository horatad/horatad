# PLATFORM — Session Handoff
# Date: 2026-05-29 (session 6 ของวัน)
# Previous: handoffs/PLATFORM_20260529_v5.md

## STATE
Branch  : claude/kind-feynman-eW98I → ff main ✅ (9933373)
Pipeline: inbox → scheduled → posted · autopost 08:30 ICT
Audit   : rigorous re-check FB auto-post → เจอ F1–F6 · แก้ F1+F2+F3 แล้ว

## DONE (session นี้ — FB auto-post rigorous audit + fix)

✓ **F1 (HIGH) — Notify on failure วางผิดตำแหน่ง** — `.github/workflows/fb_autopost.yml`
  - เดิม step "Notify on failure" อยู่ **ก่อน** "Commit moved post"
  - `if: failure()` เห็นเฉพาะ step ที่รันไปแล้ว → push-fail (โพสต์ขึ้น FB แล้วแต่ ledger ไม่ update) ไม่เคยแจ้งเตือน
  - ย้ายไปท้ายสุด + body เตือนชัดเจนเรื่อง repost risk

✓ **F2 (HIGH) — Duplicate post guard** — `workers/fb_autopost.mjs`
  - idempotency: `recentlyPostedMessages()` ดึง Page feed 24 ชม. ก่อนโพสต์
  - เจอข้อความ dup (จาก push-fail รอบก่อน) → reconcile: ย้าย scheduled/→posted/ (`reconciled:true`) ไม่โพสต์ซ้ำ
  - fail-open: ถ้าเช็ค feed ไม่ได้ → log warning + โพสต์ต่อ (กัน queue stall)

✓ **F3 (MED) — gate จุดยิงครบ 6 กฎ** — `workers/fb_autopost.mjs`
  - เดิม copy `FB_BAIT` regex inline → เช็คแค่ bait (1/6)
  - เปลี่ยนเป็น `import { fbPolicyViolations }` → เช็คครบ 6 กฎที่ปลายทางก่อนยิง (variation ข้าม recentPosts=[])
  - ลบ drift risk (bait list ซ้ำซ้อน)

verify: `node --check` ทั้ง 2 ไฟล์ ✓ · `--dry-run` ผ่าน (pick EP02) ✓ · YAML valid ✓ · step order ✓

✓ **label `alert` สร้างแล้ว** (ปีเตอร์ทำผ่าน GitHub UI · สี b60205 · verify ด้วย MCP get_label)
  - concern 8 / F1 notify ตอนนี้ติด label "alert" ให้ issue เวลา fail ได้แล้ว

✓ **F4 (MED) — weekly cap undercount + dedup window แคบ** — `workers/fb_autopost.mjs`
  - root cause: cron รันทุก 24 ชม.พอดี · GitHub delay 5-30 นาที → โพสต์รอบก่อนหลุด dedup window 24 ชม. → จับ dup ไม่ได้ → โพสต์ซ้ำ + count เกิน
  - fix: `DEDUP_WINDOW_SEC = 48 * 3600` (24→48 ชม.) ครอบ daily cron + delay · ที่ 2/วัน = ~4 โพสต์ ยังต่ำกว่า limit 25
  - วิเคราะห์: weeklyPostedCount self-heal อยู่แล้ว (FIFO หยิบเก่าสุด→reconcile ก่อนโพสต์ใหม่ · reconciled มี date_posted = ถูกนับ) · เหลือแค่ window boundary ที่ขยายแล้วปิด

✓ **Caption สะอาดสไตล์ EP05 (UX จากปีเตอร์)** — `workers/youtube_sync.mjs` `buildCaption()`
  - ปีเตอร์เทียบ EP01 (รก: ชื่อตอนซ้ำ + URL เปลือย + hashtag 4) vs EP05 (สะอาด: hook นำ → การ์ด) → เลือก EP05
  - caption ใหม่: hook นำ (fallback=title) · ตัด URL เปลือย · ตัดชื่อตอนซ้ำ (การ์ดแสดงให้แล้ว) · hashtag 3 (ตัด #HoratadAI ซ้ำชื่อเพจ)
  - migrate 96 draft เก่าในคิว (inbox 86 + scheduled 10) → สไตล์เดียวกัน (one-shot script รันแล้วลบทิ้ง)
  - EP01 (posted) record sync ด้วย · โพสต์จริงปีเตอร์ Edit เองบน FB (sandbox แตะ FB ไม่ได้ — 403) ✅ done
  - verify: body ทุกไฟล์ไม่เหลือ URL/▶️ · ไม่ขึ้นต้นด้วย EP · ไม่มี #HoratadAI

✓ **F5 ปิด — `link` param ยืนยันทำงาน (ไม่ต้องพึ่ง URL ใน body)**
  - `fb_autopost.mjs:74-76` ส่ง `params.link = meta.youtube_url` แยกต่างหาก → FB ทำ preview card เอง
  - = เลือกแบบ "จูงมือ FB" (ชัวร์กว่า auto-scan body) → ปลอดภัยที่จะตัด URL เปลือยออกจาก caption
  - ปีเตอร์ Edit EP01 จริง: การ์ดยังอยู่หลังลบ URL → ยืนยัน path ถูก

✓ **Option A — fb_post_manage (FB ops ที่ sandbox ทำไม่ได้)** — `workers/fb_post_manage.mjs` + `.github/workflows/fb_post_manage.yml`
  - ปัญหา: sandbox ต่อ FB ไม่ได้ (403) → Claude แก้/ลบโพสต์เองไม่ได้ · ต้องการ safety net เผื่อ autopost ยิงผิด
  - workflow_dispatch กดเองผ่าน GitHub Actions (runner ต่อ FB ได้ + token อยู่ใน Secrets แล้ว)
  - edit (แก้ข้อความ การ์ดอยู่ครบ) / delete (ถาวร)
  - กันชน drawback: echo-before · our-page-only (POST_ID prefix) · delete ต้อง CONFIRM=DELETE · ไม่มี schedule (คนกด) · ไม่ให้ Claude auto-trigger
  - verify: node --check ✓ · guard ผิดเพจ/ผิด action die ก่อน network ✓ · YAML keys ✓
  - note ไป GUARD แล้ว (owns workflows)

## PENDING — 🟢 Claude ทำเองได้
(ไม่มี — งาน Claude-doable ใน scope FB auto-post + caption + ops เสร็จหมดแล้ว)

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]
[ ] [ทดลองใช้] EP02 รอบถัดไปบน Page — ยืนยันสายตา caption สั้นสะอาด + การ์ดขึ้นตามสไตล์ EP05
[ ] [ทดลองใช้] ทดสอบ fb_post_manage จริง: Actions → FB Post Manage → กรอก post_id ของ posted/ → edit (ดู echo + ผลบน Page)
[ ] [ทดลองใช้] ตรวจ 08:30 ICT วันถัดไปว่า EP ขึ้น Page จริง (ยืนยัน pipeline + dedup ไม่ false-block)
[ ] [ทดลองใช้] ทดสอบ F1: trigger workflow fail ที่ step commit → ตรวจว่า issue ถูกสร้าง
[ ] [ทดลองใช้] fb_post_helper backlog section แสดงถูก

## 🔭 VISION — Combined post (คลิป + วิจัย ใน 1 โพสต์) — ปีเตอร์ยืนยัน 2026-05-29
เป้าหมาย: FB feed = ผสมคลิปสั้น (NotebookLM→YouTube) + งานวิจัย JULIAN
รูปแบบ: **1 โพสต์ = ข้อความวิจัย (ผ่าน gate) เป็น caption + คลิป YouTube เป็นการ์ด (link param)**
- คนหยุดดูเพราะคลิป → อ่านวิจัยต่อ · คนสนใจวิจัย → ดูคลิป (เสริมกัน + ดีต่อ reach)
- กลไกประกอบ: PLATFORM หยิบข้อความวิจัยที่**ผ่าน policy gate แล้ว** (JULIAN buildResearchCaption/buildFBCaption) + เซ็ต link=คลิป · **ไม่เขียน caption วิจัยในขั้นประกอบ** (JULIAN FB Rule)
- จุดยาก = จับคู่คลิป↔วิจัย → **แก้แล้วโดย design**: คลิปเกิดมาเพื่อ finding ชิ้นนั้น → ติด ID/หัวข้อตอนสร้าง = จับคู่แม่น 100% ไม่ต้อง fuzzy-match
- คุม Page health: เพดานรวม 2/วัน อย่ายิงถี่เกิน
- ⚠️ คาบเกี่ยว PLATFORM (ประกอบ+โพสต์+scheduler mix) + JULIAN (เนื้อวิจัย/gate) → **ควรเปิด session BIG วางลำดับก่อน**
- ✅ ปิดคำถามค้าง (2026-05-29): คลิปสั้นมี **2 ชนิด ผูกกับวิจัยโดยตรง** → แมตช์ caption JULIAN ที่มีอยู่แล้วเป๊ะ:
  - **คลิปชนิด 1 = interesting issue + key finding** ↔ `buildFBCaption()` (statistical: n=, p-value, disclaimer) → โพสต์รวม **แบบ A**
  - **คลิปชนิด 2 = background ของ issue** (เรามาเจอประเด็นนี้ยังไง + ความรู้ทั่วไป) ↔ `buildResearchCaption()` (research/BTS: ไม่ต้องมี n=/p) → โพสต์รวม **แบบ B**
  - implication: ไม่ต้องสร้าง gate ใหม่ · PLATFORM แค่ประกอบ A/B · สลับ A/B = feed หลากหลาย ดีต่อ reach

## DEFERRED — รอ user / dependency
[ ] F6 (LOW) cron ordering: youtube_sync 09:00 ICT รัน **หลัง** curator (08:00)+autopost (08:30)
    → วิดีโอ sync วันนี้ curate พรุ่งนี้ (lag 1 วัน · มี backlog buffer ไม่พัง) · ถ้าจะแก้ ย้าย youtube_sync ก่อน 08:00
[ ] `/published_posts` vs `/posts` KPI issue · `read_insights` scope (≥100 followers) · Token Data Access re-auth (ปลาย ส.ค. 2026)

## WHY LOG
- **F1 ย้าย Notify ท้าย workflow (2026-05-29)** — `if: failure()` ใน GitHub Actions ประเมินตอนถึง step นั้น เห็นเฉพาะ step ก่อนหน้า · Notify ที่อยู่ก่อน Commit จึง miss push-fail = failure mode อันตรายสุด (เงียบ → repost) · ย้ายท้ายสุดจับครบทุก step
- **F2 reconcile ไม่โพสต์ซ้ำ + fail-open (2026-05-29)** — external side-effect (FB post) ผูก atomic กับ git push ไม่ได้ · idempotency guard เช็ค feed ก่อนยิงคือทางแก้ที่ practical สุด · เจอ dup = reconcile ledger ไม่ใช่โพสต์ใหม่ · fail-open เพราะ block ทุกครั้งที่ API flake = queue หยุด แย่กว่า window ซ้ำที่สั้นมาก
- **F3 import แทน copy (2026-05-29)** — concern 1+2 รวม gate ให้ curator+requeue แต่ลืมจุดยิงจริง · fb_autopost copy แค่ bait = defense-in-depth ครอบ 1/6 + drift · import ครบทั้งระบบ fix ที่เดียว
- **Caption ตัด URL เปลือย (2026-05-29)** — การ์ดวิดีโอมาจาก `link` param ที่ fb_autopost ส่งแยก ไม่ใช่จาก URL ใน body · URL ใน text จึงซ้ำซ้อน + รก ไม่ช่วยคนอ่าน · hook นำดึงความสนใจดีกว่าชื่อตอน (ชื่อตอน+thumbnail อยู่ในการ์ดแล้ว) · ผลที่ปีเตอร์เห็น = โพสต์สั้น สะอาด เหมือน EP05
- **EP01 Edit ไม่ลบ-โพสต์ใหม่ (2026-05-29)** — โพสต์ที่ live แล้วมี engagement+อายุ · Edit ข้อความไม่ทำให้การ์ดที่ผูกไว้หาย → แก้ text ได้โดยเก็บ like/comment ครบ · ลบ+โพสต์ใหม่ = ทิ้ง engagement เปล่า · Claude แก้ FB เองไม่ได้ (sandbox 403) → ให้ปีเตอร์กดเอง พร้อม text สำเร็จรูป

## 📨 Incoming — note ไป GUARD
- session นี้แก้ `.github/workflows/fb_autopost.yml` (GUARD owns workflows) — เป็น PLATFORM pipeline workflow (F1 reorder notify step) · ไม่กระทบ security posture · note ใน GUARD handoff แล้ว
