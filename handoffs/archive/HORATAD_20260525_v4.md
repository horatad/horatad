# HORATAD — Session Handoff
# Date: 2026-05-25 (session 4 ของวัน)
# Previous: handoffs/archive/HORATAD_20260525_v3.md

## STATE
App version : V3.3.36 (main + deployed)
Backups     : backup/v3.3.33, backup/v3.3.34, backup/v3.3.36

## DONE (session นี้ 2026-05-25 v4)
✓ **ESLint dead code cleanup** — `v3/interpretation.js` + `v3/typhoon.js`
  - ลบ unused import `get_all_houses`, dead vars `houses` / `h10Planets` / `h10Owner*`
  - ลบ unused `pSign` var, `catch(err)` → `catch(_err)` parse block
✓ **Wire matchRulesV24 + kb_v24-3** เข้าปุ่ม "ดูกฎที่ตรงดวง" (V3.3.25)
  - `v3Local()` ใช้ `buildNatalState` + `matchRulesV24` + `_loadKbV24()`
  - `_renderV24()` render r.meaning grouped by polarity+domain
  - SW CORE_ASSETS เพิ่ม `kb_v24-3.json`
✓ **Speak-rate toggle** 1×/1.25×/1.5×/1.75× + localStorage persist (V3.3.26)
  - ปุ่ม `#v3-rate-btn` + `window.v3CycleRate()` + `_syncRateBtn()`
  - `v3Speak()` strips `[R\d+]` rule IDs ก่อน speak
✓ **Voice chat real-time** push-to-talk STT→Typhoon→TTS (V3.3.27)
  - `send_chat(messages[])` ใน typhoon.js
  - `_vcSystemPrompt()` inject lagna + KB rules context
  - `_vcBuildRulesCtx()` top-20 NATAL rules เป็น bullet list
  - `v3MicToggle()` Web Speech API th-TH
✓ **Mode buttons auto-refresh** — กดปุ่มเลือก mode → แสดงผลเลย ไม่ต้องกดปุ่มซ้ำ (V3.3.35)
✓ **TTS ไม่อ่าน rule ID** — strip `[R01]` ก่อน speak (V3.3.35)
✓ **KB rules inject voice chat** — `_vcRulesCtx` ใน system prompt (V3.3.35)
✓ **restore เกาะในฝัน BGM** บนหน้า About — V3.3.36
  - ถูกลบโดยไม่ตั้งใจใน V3.3.34 (overshoot task)
  - คืน: audio element + button (index.html), CSS .about-bgm-btn (style.css), toggleAboutBgm() (script.js)
✓ **HORATAD_memory.md** — mark `about-bgm` PROTECTED + บันทึก BGM overshoot lesson

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

[ ] **Phase 2 Step 1 — core/ Tier 1 split** — ขึ้นต่อ CSP enforce P2-A (รอ violations 1 wk)
[ ] **Phase 2 Step 2 — db/ Tier 2 split** — รอ Step 1
[ ] **Retroactive backup** backup/v3.3.25–v3.3.32 — SHA ยังหาได้จาก git log (งานเล็ก ทำได้เลย)

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ⭐ ทดสอบ V3.3.36 — BGM เกาะในฝัน หน้า About กดปุ่มเล่น/หยุดได้ปกติ
[ ] [ทดลองใช้] ⭐ ทดสอบ V3.3.27 voice-chat — push-to-talk + Typhoon reply + TTS speak (ครบ flow)
[ ] [ทดลองใช้] ⭐ ทดสอบ mode buttons auto-refresh (V3.3.35) — กด ดวงเดิม/ดวงจร/ทั้งคู่ → แสดงผลทันที
[ ] [ทดลองใช้] ⭐ Lighthouse mobile + desktop — checklist `docs/admin/USER_TASKS_lighthouse_csp_2026-05-24.md` Part 1
[ ] [ทดลองใช้] CSP violations 1 wk (deadline ~2026-05-30) → Part 2
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy Option A/B/C → GUARD R-07
[ ] [BLOCKED] julian_all.json move target — รอ user เลือก
[ ] [BLOCKED] CSP enforce (P2-A) — รอ browser violations 1 wk Report-Only

## DEFERRED
[ ] **PROVINCES extract** — hot calc path, รอ module split

## WHY LOG
- **about-bgm PROTECTED** — ถูกลบโดยไม่ตั้งใจใน V3.3.34 เพราะ task "ลบ lunar BGM" overshoot
  กฎ: task BGM ต้องระบุ element ID ชัด (about-bgm vs lunar-bgm) + review diff ก่อน commit
- **V3.3.34 ลบ lunar BGM** — user ทดสอบแล้วรู้สึกว่าเพลงหน้าจันทรคติไม่จำเป็น + info bar รกหน้า
- **kb_v24-3 wire local only** — `v3Typhoon()` ยังใช้ kb.json V2.3 (ต้องการ r.p field) → 2 paths คู่กันไม่ break
- **voice-chat max_tokens=400** — reply สั้นพอ TTS speak ไม่นานเกิน
- **BGM auto-play risk iOS** — switchTab(2) = tap = user gesture ผ่าน policy (แต่ต้องทดสอบจริง)
