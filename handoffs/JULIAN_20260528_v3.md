# JULIAN — Session Handoff
# Date: 2026-05-28 (session 3 ของวัน)
# Previous: handoffs/JULIAN_20260528_v2.md → archive/

## STATE
Records       : 98,437 (Sync รัน ล่าสุด 05:37 UTC — รอ trigger อีกรอบ)
Data repo     : horatad/julian-data ✅
Workflows     : 3 ไฟล์ (Sync / Events / MusicBrainz)
Auto-chain    : Sync → Backfill → Index ✅ (jobs ใน sync.yml)
PAT secret    : JULIAN_DATA_PAT ✅
Index files   : planet_sign + by_year + genealogy.json + succession.json (รอ workflow run)

## DONE (session นี้ — หลัง v2)

✓ **Image feature (Wikimedia Commons P18)**
  - `workers/julian_scraper.mjs` — inject ?img ทุก SPARQL query (auto), store filename
  - `workers/julian_index_builder.mjs` — `g` field ใน mini record, build `images.json`
    (threshold: sitelinks≥20 หรือ importance≥0.4 → คาด 5-15K records)
  - `tools/julian_lookup.js` — `loadImageIndex()` + `getImageUrl(qid, width=120)`
  - URL pattern: `https://commons.wikimedia.org/wiki/Special:FilePath/{filename}?width=120`
  - ⚠️ records 98K ที่มีอยู่ยังไม่มี img — จะเติมเมื่อ rescrape (ทำครั้งละ batch)

✓ **Genealogy index (Wikidata P22/P25/P26/P40/P3373)**
  - `workers/julian_genealogy_backfill.mjs` — batch 50 QID, output `data/julian_family.json`
    { QID: {p, m, sp:[], ch:[], si:[]} } — raw map ทุก relation
  - `workers/julian_index_builder.mjs` — cross-ref เฉพาะ QIDs ที่อยู่ใน DB → `data/index/genealogy.json`
  - `tools/julian_lookup.js` — `loadGenealogyIndex()` + `getFamily(qid)`
  - `.github/workflows/julian_sync.yml` backfill job — รัน genealogy หลัง P3447
  - ใช้: วิเคราะห์ดาวครอบครัว (พ่อ/แม่/คู่/ลูก/พี่น้อง) เทียบ natal chart

✓ **Succession index (Wikidata P39+P1365/P1366)**
  - `workers/julian_succession_backfill.mjs` — batch 50 QID, output `data/julian_succession.json`
    { QID: {prev:[{q,pos}], next:[{q,pos}]} } — pos = QID ของตำแหน่ง
  - `workers/julian_index_builder.mjs` — cross-ref → `data/index/succession.json`
  - `tools/julian_lookup.js` — `loadSuccessionIndex()` + `getSuccession(qid)`
    แก้ bug `_fetchJson` → `_fetchJSON` ใน loadGenealogyIndex + loadImageIndex ด้วย
  - `.github/workflows/julian_sync.yml` backfill job — รัน succession หลัง genealogy
  - ครอบทุกประเภทตำแหน่ง: นายกฯ, ประธานาธิบดี, นางงาม, แชมป์มวย, แชมป์หมากรุก ฯลฯ
  - Checkpoint: `workers/julian_succession_checkpoint.json`

## PENDING — 🔴 [ทดลองใช้] รอ user trigger

[ ] **[ทดลองใช้] Trigger "JULIAN Data Sync"** (workflow_dispatch)
    → auto-chain: Sync → Backfill (P3447+genealogy+succession) → Index rebuild
    → หลัง run: `data/julian_family.json` + `data/julian_succession.json` จะอยู่ใน data repo
    → `data/index/genealogy.json` + `data/index/succession.json` + `images.json` ปรากฏใน index
    ⚠️ img field จะเติมเฉพาะ records ที่ scrape ใหม่หลัง feature นี้ (98K เก่าไม่มี)

[ ] **[ทดลองใช้] Trigger "JULIAN MusicBrainz Scraper"** — ทดสอบ PAT fix + fetch+reset commit

[ ] **[ทดลองใช้] ตรวจ genealogy.json + succession.json** หลัง Sync run
    - ดู data/index/meta.json → genealogy_count + succession_count ต้องมีค่า > 0
    - ดู julian_lookup.js: `getFamily('Q937')` (Einstein) ควรได้ sp/ch

## PENDING — 🟢 Claude ทำเองได้ (รอ data จาก workflow)

[ ] ตรวจ full_scan 50 significant combinations — กี่อันเป็น MR-related?
    (ml/models/julian_full_scan.json — อ่านได้ตอนนี้ถ้าไฟล์มีอยู่)
[ ] SU×SA deeper investigation — degree-level (รอ accuracy A/B/C records จาก backfill)
[ ] เพิ่ม `getRecordByQid(qid)` ใน julian_lookup.js — search mini records จาก cache
    (useful สำหรับ resolve succession/genealogy QIDs → name + year + image)

## DEFERRED

[ ] Full rescrape events (batches 0-704 ขาด marriage/graduation/child_birth)
    วิธี: ลบ julian_events.jsonl + checkpoint → trigger Events workflow ใหม่
    ต้องการ: user กด trigger + ยืนยัน data loss ของ events เก่า
[ ] [ทดลองใช้] `tools/julian_seed_input.html` — กรอก records accuracy A/B
[ ] [ทดลองใช้] BIBLE session: apply SU×SA polarity → empirical_p ใน kb.json
    ⚠️ ต้องเอา MR×MR rules ออกก่อน (DEBUNKED: MR period=83yr=age proxy)

## DATA FILE MAP (อัปเดต)

```
data/julian_all.json          — main DB (98K records)
data/julian_family.json       — genealogy raw (P22/P25/P26/P40/P3373) [ยังไม่มี รอ backfill]
data/julian_succession.json   — succession raw (P39+P1365/P1366) [ยังไม่มี รอ backfill]

data/index/
  meta.json                   — routing map + stats (total, year_range, counts)
  planet_sign/{PL}/{SIGN}.json — mini records (q,n,j,y,a,i,c,s,g)
  by_year/{YYYY}.json          — full records ≥1900
  by_decade/{NNNN}s.json       — 1700-1899
  by_50yr/{NNNN}-{NNNN}.json   — <1700
  images.json                  — { QID: filename } notable persons [ยังไม่มี รอ index rebuild]
  genealogy.json               — { QID: {p,m,sp,ch,si} } in-DB only [ยังไม่มี]
  succession.json              — { QID: {prev:[{q,pos}], next:[{q,pos}]} } in-DB only [ยังไม่มี]
```

## LOOKUP API SUMMARY (tools/julian_lookup.js)

```javascript
const lookup = new JulianLookup();
await lookup.queryPlanetSign('SA', 10)   // Saturn in Kumbha (Aquarius)
await lookup.queryYear(1955)             // ทุกคนเกิดปี 1955
await lookup.searchByName('Einstein')   // search จาก cache ที่โหลดไว้
await lookup.getImageUrl('Q937', 120)   // Einstein photo URL
await lookup.getFamily('Q937')          // { father, mother, spouses, children, siblings }
await lookup.getSuccession('Q1124')     // { prev:[{q,pos}], next:[{q,pos}] }
```

## ⛔ MR×MR DEBUNKED (standing rule)
MR period = 83.03yr = age proxy — ห้ามใช้ใน rules ทุกกรณี

## KEY FINDINGS
### ✅ SU×SA Polarity (valid, รอ degree-level ก่อน production)
| Rule | Event | Lift |
|---|---|---|
| transit_SU hard natal_SA | death | ~1.12–1.18 |
| transit_SU soft natal_SA | award | ~1.12–1.16 |

## WORKFLOW ARCHITECTURE (อัปเดต)
```
julian_sync.yml (ทุก 6 ชั่วโมง + manual)
  job sync       → Wikidata scrape + enrich (Astrotheme/WikiTH/Coord) + stats
  job backfill   → P3447 + regrade + genealogy (P22/P25/P26/P40/P3373) + succession (P39+P1365/P1366)
  job build-index → planet_sign/by_year/genealogy/succession/images index

julian_events.yml (4x/day auto)
julian_musicbrainz.yml (weekly + manual)
```

## WHY LOG
- **images เฉพาะ notable persons** — threshold sitelinks≥20 ป้องกันไฟล์ใหญ่เกิน (98K records มีรูปทั้งหมด ~30MB)
- **genealogy/succession cross-ref เฉพาะ in-DB** — QID นอก DB ไม่มีข้อมูลดาว = ไม่มีประโยชน์ต่อ analysis
- **succession ใช้ P39+qualifier** — P1365/P1366 ไม่ใช่ direct property แต่เป็น qualifier บน P39 statement → ต้อง query ผ่าน `p:P39 → pq:P1365/pq:P1366`
- **แก้ bug _fetchJson** — loadGenealogyIndex + loadImageIndex เรียก `this._fetchJson()` ผิด (camelCase) → `_fetchJSON` ถูก — fix แล้วในเดียวกับ succession commit
