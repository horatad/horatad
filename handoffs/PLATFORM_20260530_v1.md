# PLATFORM — Session Handoff
# Date: 2026-05-30 (session 1 ของวัน)
# Previous: handoffs/archive/PLATFORM_20260529_v7.md

## STATE
App     : FB content pipeline (inbox → scheduled → posted) · autopost cron 08:30 + 11:30 ICT
Branch  : claude/platform-RvVL3 → ff main ✅ (1e962bc)
Backups : backup/fb-autopost-cron-fix
Scope   : ซ่อม autopost cron drop + demand signal scan + R-22 hardening (อนุญาตแตะ workflow GUARD ครั้งนี้)

---

## DONE (session นี้ — 2026-05-30)

✓ **Root cause: ไม่มีโพสต์ FB วันนี้ (05-30)** — cron หลัก 01:30 UTC ไม่ firing (GitHub schedule best-effort drop)
  ตัดตัวเลือก: คิวไม่ว่าง (10), cap 2/14, ไม่มี failure issue → workflow ไม่ได้รัน

✓ **แก้ cron drop** (commit 6d1f1fb):
  - `fb_autopost.mjs`: เพิ่ม `--catch-up` guard (โพสต์เฉพาะวันที่ยังไม่มีโพสต์ออก → กันซ้ำ 2 รอบ/วัน)
  - `fb_autopost.yml`: เพิ่ม backup cron `30 4 * * *` (11:30 ICT) รัน `--catch-up`
  - `fb_autopost.yml`: `permissions` += `issues: write` — ⚠️ เดิมขาด → `gh issue create` 403 เงียบ (Notify-on-failure เสียมาตั้งแต่ GUARD ใส่ permissions block)
  - `fb_autopost.yml`: token หาย + คิวมีของ → สร้าง alert issue (กันหายเงียบทั้งวัน)

✓ **FB Insights Phase 0 — Demand Signal Scan** (commit 36108f5) → `docs/PLATFORM_STRATEGY.md`
  4 cluster: มูมวล(🔴 อิ่มตัว) · เรียนผูกดวง(🟡 monetized) · "ตรวจสอบได้"(🟢 active+ไร้เจ้าของ=ICP เรา) · AIโหรา(🟡 คู่แข่งจริง)
  Key: gap = cluster skeptic ที่ไม่มีแบรนด์ตอบด้วยข้อมูลจริง · คู่แข่งจริง = AI โหราศาสตร์ ไม่ใช่หมอดู

✓ **R-22 (P3) hardening** (commit 1e962bc): FB token → `Authorization: Bearer` header ทุก call
  fb_autopost / fb_kpi_report / fb_post_manage — ไม่เหลือ access_token ใน query/body

✓ note cross-project ลง `handoffs/GUARD_20260529_v4.md` (แตะ workflow + R-22 done)

✓ **ปิด thread "silent-403 alert"** (commit 4016a2c) — ตรวจ fb_post_manage/fb_kpi_monthly: ไม่มี `gh issue create` → ไม่มีบั๊กเดียวกัน (unique เฉพาะ fb_autopost). พ่วงแก้ `fb_kpi_report.mjs` Graph v19.0 → GRAPH_VERSION||v23.0 (align siblings, เลิก hardcode เก่า)

---

## PENDING — 🟢 Claude ทำเองได้ (sandbox)
(หมดงาน Claude-doable ฝั่ง PLATFORM รอบนี้ — ที่เหลือเป็น cross-project หรือ verify จริง)

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]
[x] ~~[ด่วนวันนี้] กู้โพสต์ 05-30 ด้วยมือ~~ ✅ พี่ปีเตอร์ trigger แล้ว (commit bff869a, 09:26 UTC = manual dispatch) — โพสต์ออกเรียบร้อย
[ ] [ทดลองใช้] verify backup cron 11:30 ICT ทำงานจริงในวันที่รอบหลัก drop (ยังไม่เกิดเคส drop จริงให้ทดสอบ)
[ ] [ทดลองใช้] verify token-missing alert สร้าง issue จริง (ทดสอบ: ลบ secret ชั่วคราว/dry)
[ ] [ทดลองใช้] verify R-22 header refactor โพสต์/edit/delete/kpi จริงผ่าน (ต้อง token + network)
[ ] [ทดลองใช้] ต่อจาก v7: EP02 รอบถัดไป — caption สะอาด + การ์ดขึ้น
[ ] [ทดลองใช้] tagline สุดท้าย — รอพี่ปีเตอร์ตกผลึก paradox
[ ] [ทดลองใช้] เพิ่ม read_insights scope ตอน regenerate token เมื่อ followers ≥100

## DEFERRED — รอ decision / dependency
[ ] cohort builder "โหราทาส Investigates" — JULIAN scope (เปิด session JULIAN)
[ ] gen_project_status.mjs — BIG scope
[ ] FB Insights own-page loop (Phase 1-3) — รอ followers ≥100
[ ] Combined post (คลิป YT + วิจัย JULIAN) — รอ BIG วางลำดับ
[ ] F6 cron ordering: youtube_sync ย้ายก่อน 08:00

---

## WHY LOG

- **backup cron แทนการ retry รอบเดียว (2026-05-30)** — GitHub schedule เป็น best-effort, drop เงียบได้ทั้งวัน. รอบสำรอง 11:30 + `--catch-up` guard = ปลอดภัยกว่า (รอบหลักสำเร็จ → สำรองข้าม, รอบหลัก drop → สำรองกู้) โดยไม่เสี่ยงโพสต์ 2 ครั้ง/วัน

- **`issues: write` หาย = alert path ตายเงียบ (2026-05-30)** — GUARD ใส่ `permissions:` block (least-privilege) แต่ลืม issues:write → Notify-on-failure ที่ `gh issue create` จะ 403 มาตลอด. บทเรียน: ใส่ permissions block ต้องนับ scope ของ "ทุก step" รวม alert/notify ไม่ใช่แค่ happy path

- **คู่แข่งจริง = AI โหราศาสตร์ ไม่ใช่หมอดู (2026-05-30)** — demand scan เจอ Destiny Architect/ChatGPT ดูดวง เคลม "AI objective กว่าหมอดู". เราต่างตรง "กล้าบอก pattern อ่อน + control group" — ถ้าไม่ชู differentiator นี้จะถูกกลืนเป็น "อีกเจ้า AI ดูดวง"

- **gap = cluster 'ตรวจสอบได้' ที่ไร้เจ้าของ (2026-05-30)** — Pantip/มติชน/ThaiPBS ถาม "ดูดวงพิสูจน์ได้ไหม" ต่อเนื่องแต่ไม่มีแบรนด์ตอบด้วยข้อมูลจริง = ช่องที่ positioning "สะพาน" เสียบพอดี

---

## 📨 Incoming from GUARD (2026-05-29) — ค้างเดิม (ไม่เปลี่ยน)
R-22 ✅ ปิดแล้ว session นี้. ที่เหลือ: ดู `docs/cia/fb_pipeline_audit_2026-05-29.md` — ไม่มี critical/P0 ค้าง
