# HORATAD — Session Handoff
# Date: 2026-05-25 (session 3 ของวัน)
# Previous: handoffs/archive/HORATAD_20260525_v2.md

## STATE
App version : V3.3.34 (main + deployed)
Backups     : backup/v3.3.33, backup/v3.3.34
              ⚠️ V3.3.25–V3.3.32 ไม่มี backup branch (sessions ก่อนหน้าลืม)

## DONE (session นี้ 2026-05-25 v3 — fingerprint scanner)
✓ สร้าง fingerprint scanner project ทั้งหมดใน repo (ชั่วคราว — รอ BIG ย้าย)
  - `capture.ps1` — PowerShell bridge → DigitalPersona One Touch SDK (.NET) → capture PNG
  - `server.js` — Express gallery server port 3000 (ไม่ใช่ horatad server)
  - `fp_compare.py` — OpenCV ORB feature matching + visual output
  - `fp_compare.js` — Node.js attempt (abandoned)
  - `fp_gallery.html` — gallery UI เก่า (superseded โดย server.js)
✓ แก้ปัญหาทั้งหมดจนระบบทำงานได้ครบ:
  - Hidden form → SDK events ไม่ fire → แก้: form visible + TopMost
  - Thai text → cp1252 encoding error → แก้: เปลี่ยนเป็น English
  - AbortSignal.timeout → Chrome ไม่รองรับ → แก้: ลบออก
  - innerHTML + template literal → SyntaxError → แก้: DOM API
  - sourceafis ไม่มีบน PyPI → แก้: ใช้ OpenCV ORB แทน
  - UnicodeEncodeError (✓ ✗) → แก้: ใช้ ASCII "MATCH" / "NO MATCH"
✓ แจ้ง BIG handoff — fingerprint files ต้องแยกออกเป็น repo ใหม่ `fingerprint-scanner`

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

[ ] **Phase 2 Step 1 — core/ Tier 1 split** — ขึ้นต่อ CSP enforce P2-A (รอ violations 1 wk)
[ ] **Phase 2 Step 2 — db/ Tier 2 split** — รอ Step 1
[ ] **PROVINCES extract** — DEFERRED (hot calc path)
[ ] **Retroactive backup** backup/v3.3.25–v3.3.32 — SHA ยังหาได้จาก git log (งานเล็ก ทำได้เลย)

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ⭐ ทดสอบ V3.3.34 — ลบเพลงหน้าจันทรคติ + ไม่มี info bar about page
[ ] [ทดลองใช้] ⭐ ทดสอบ BGM about page — "เกาะในฝัน" เล่น silent (ไม่ต้องแสดง UI)
[ ] [ทดลองใช้] ⭐ Lighthouse mobile + desktop — checklist `docs/admin/USER_TASKS_lighthouse_csp_2026-05-24.md` Part 1
[ ] [ทดลองใช้] CSP violations 1 wk (deadline ~2026-05-30) → Part 2
[ ] [ทดลองใช้] ⭐ ทดสอบ V3.3.27 voice-chat — push-to-talk + Typhoon reply + TTS speak (ครบ flow)
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy Option A/B/C → GUARD R-07
[ ] [BLOCKED] julian_all.json move target — รอ user เลือก
[ ] [BLOCKED] CSP enforce (P2-A) — รอ browser violations 1 wk Report-Only

## 📨 Incoming from BIG (2026-05-24 — ยังคงค้าง)
`script.js` 24 ESLint warnings — false positives จาก inline handlers — จะหายเองหลัง CSP P2-A refactor

## WHY LOG
- **Fingerprint project ใน horatad repo (ชั่วคราว)** — สร้างระหว่าง session เพราะไม่มี repo แยก → BIG รับทราบแล้ว จะย้ายใน session ถัดไป
- **ORB แทน minutiae** — sourceafis ไม่มีบน PyPI (Java/.NET only) → ORB ใช้งานได้ทันที threshold 15% = MATCH ทำงานได้กับ U.are.U 4500
- **PowerShell -STA flag จำเป็น** — Windows Forms ต้องการ Single Thread Apartment, ขาดแล้ว SDK events ไม่ fire
- **Form ต้องมองเห็นได้** — hidden/minimized form ทำให้ Windows throttle hardware events → form visible + TopMost เป็น workaround
- **Two-touch pattern** — ฮาร์ดแวร์ behavior ของ U.are.U (touch → detect → lift → touch+hold → OnComplete) ไม่ใช่ bug
- **V3.3.34 ลบ lunar BGM** — user ทดสอบแล้วรู้สึกว่าเพลงหน้าจันทรคติไม่จำเป็น + info bar แสดงชื่อเพลงรกหน้า
- **V3.3.33 BGM overlap fix** — switchTab ต้อง stop ทั้งสองเพลงเมื่อออกจาก tab 2
