# BIG — Cross-Project Coordinator Session
# Date: 2026-05-29 (session 1 ของวัน)
# Previous: handoffs/archive/BIG_20260525_v2.md

## STATE

| Project  | Version / Status |
|---|---|
| HORATAD  | V3.3.34 — main + deployed ✅ |
| BIBLE    | KB V2.3 prod · 290 rules · schema v2.1 + tals_lagna.json ✅ |
| JULIAN   | 58,428/100,000 records (58.4%) · automation cron 6 ชม. ✅ |
| NOK      | Phase 1 deployed (TTS in HORATAD V3 tab) |
| GUARD    | Phase 1 = 6/7 done · P1-E blocked R-07 · _headers gap DEFERRED |
| BIG      | admin tooling live · deployment health check เพิ่มแล้ว |

main @ b926eeb · working tree clean ✅

---

## DONE (session นี้ — 2026-05-29)

### Deployment health audit + fixes
✓ วิเคราะห์ `pages-build-deployment` workflow — 613 runs ล้มเหลวตั้งแต่ 16 พ.ค.
✓ Root cause: ไม่มี `.nojekyll` → Jekyll พยายาม build → fail ทุก commit
✓ Root cause ที่ถี่ขึ้น: `ff_main.sh` standing rule (23 พ.ค.) เพิ่ม commit frequency 10x
✓ เพิ่ม `.nojekyll` → Pages build จะผ่านตั้งแต่ commit ถัดไป
✓ `big_status.mjs` section 6 "Deployment Health" — ตรวจ .nojekyll + CNAME + production checklist
✓ `CLAUDE.md` — เพิ่ม "🏥 Production Health Checklist" ใน BIG SESSION section (3 กฎ)
✓ GUARD handoff — บันทึก `_headers` gap + เปลี่ยน status → DEFERRED

### Gap พบ (บันทึก WHY LOG)
- `_headers` (Cloudflare Pages format) ไม่ถูก apply บน GitHub Pages — headers ทั้งหมดที่ GUARD เขียนไว้ไม่ active จริง
- CSP Report-Only ไม่เคยถูกส่งไปยัง browser → user task "เก็บ violations 1 wk" เก็บได้ 0 เพราะ header ไม่มีอยู่จริง
- แก้ด้วย Cloudflare Transform Rules — user ตัดสินใจ DEFER ไว้ก่อน (ทำเมื่อ user base ขยาย)

---

## PENDING — 🟢 Claude ทำเองได้

ไม่มีงานค้างใหม่จาก session นี้

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] R-07 decision (GUARD recommends Option C) → unblocks P1-E + BIBLE QR privacy
[ ] [ทดลองใช้] Lighthouse mobile + desktop → `docs/admin/USER_TASKS_lighthouse_csp_2026-05-24.md`
[ ] [ทดลองใช้] CSP violations collection — **หมายเหตุ: ยังทำไม่ได้** จนกว่าจะย้าย headers ไป Cloudflare
[ ] [ทดลองใช้] ลบ 6 merged claude/* branches (sandbox 403)

---

## DEFERRED

[ ] Cloudflare Transform Rules — ย้าย headers จาก `_headers` → ทำเมื่อ user base ขยาย หรือ security incident จริง

---

## WHY LOG

- **Production Health Checklist เพิ่มใน CLAUDE.md (2026-05-29)** — BIG ตรวจ code state ดีแต่ไม่ตรวจ production state มาตลอด gap นี้ทำให้ (1) Pages fail 613 ครั้งโดยไม่รู้ (2) security headers ไม่ active แม้เขียนครบ แก้ด้วย checklist 3 ข้อบังคับก่อน/หลัง protocol change

- **`_headers` DEFERRED ไม่ใช่ลบทิ้ง (2026-05-29)** — security headers มีประโยชน์แต่ impact ต่ำสำหรับ app ที่ยังไม่มี user base ใหญ่ ลำดับความสำคัญต่ำกว่า feature ใหม่มาก trigger ย้าย = user base ขยาย / มี security incident จริง

- **CSP violations task เป็น false premise (2026-05-29)** — task "เก็บ violations 1 wk" สร้างขึ้นบนสมมติว่า CSP Report-Only header ถูกส่งจริง แต่ไม่ถูกส่งเลยตั้งแต่ต้น → task นี้ block ไว้จนกว่าจะแก้ headers ก่อน

---

## ✅ RESOLVED — "BIBLE branch drift" เป็น false alarm (debunked 2026-05-29 BIG session 6)

~~🔴 BIBLE branch drift — 3 branches divergent history (343/312/323 unmerged)~~

**ความจริงหลัง `git fetch --unshallow`** (ตัวเลขเดิมเพี้ยนเพราะ shallow clone `--depth 50` ตัด common ancestor):

| branch | ahead จริง | สถานะ | action |
|---|---|---|---|
| `claude/bible-NuznI` | **0** | merged หมดแล้ว | 🗑 ลบได้ (ปีเตอร์ทำใน GitHub UI — sandbox 403) |
| `claude/eager-knuth-aHdsK` | **0** | merged หมดแล้ว | 🗑 ลบได้ |
| `claude/affectionate-cray-T7HUu` | **1** | commit เดียว `5e03649 batch3 R156-R191` — main ไปถึง 290 rules แล้ว น่าจะ superseded | ⭐ BIBLE verify content ก่อนทิ้ง |

→ ไม่ใช่ 978 commits divergent · ไม่ต้อง investigate ใหญ่ · เป็น shallow artifact (ดู hook fix `ec52401`)

---

## Priority Queue (cross-project — ไม่เปลี่ยนแปลงจาก v2)

| # | Project | งาน | Score | Blocker |
|---|---|---|---|---|
| 1 | GUARD | P1-E ship (R-07 Option C) | 5 | user R-07 decision |
| 2 | BIBLE | Groq + Typhoon extract + review 32 dupes | 3 | user browser run |
| 3 | BIBLE | Fill master_dict 5 remaining sections | 2 | Claude ทำเองได้ |
| 4 | JULIAN | Phase 3 validate.mjs | 1 | รอ GUARD patch + raw accumulate |
| 5 | BIBLE | branch drift investigate (3 branches) | 1 | cold storage |

## 📨 Incoming from JULIAN (2026-05-29)
- `julian_stats.json` ตอนนี้มี fields ใหม่ `genealogy` + `succession` (วางบนสุดก่อน `total`)
- **ขอ:** อัปเดต `.claude/hooks/session-start.sh` (JULIAN stats block ~line 77-108) ให้แสดง genealogy/succession **ก่อน** record count — ปีเตอร์กำหนดว่า genealogy = research asset อันดับ 1 ต้องรายงานก่อนจำนวนข้อมูล
- field ที่ใช้: `genealogy.in_db_links`, `genealogy.in_db_persons`, `succession.in_db_links` (ดู workers/julian_stats.mjs)

## 📨 Incoming from PLATFORM (2026-05-29) — hook false-alarm fix
- **แก้ `.claude/hooks/session-start.sh` ให้ shallow-aware** (PLATFORM session แตะ BIG file — ปีเตอร์สั่งตรง)
- **Root cause:** container clone repo แบบ shallow `--depth 50` → `.git/shallow` boundary ตัด common ancestor → `git log origin/main..HEAD` นับทั้ง history ที่ถูกตัด = 50 → ขึ้น "⚠ commits ค้าง 50" ทั้งที่ HEAD == origin/main จริง (พิสูจน์: `git fetch --unshallow` แล้ว origin/main เด้งเป็น e263bd2 = HEAD, ahead 0/behind 0)
- **Fix:** เพิ่ม guard 3 ชั้น — (1) ถ้า `rev-parse HEAD == origin/main` → synced เลย (2) ถ้า shallow + ไม่มี merge-base → PENDING="?" แสดง "ℹ shallow clone — ข้ามนับ" + แนะ `git fetch --unshallow` (3) full clone → นับปกติ · contamination check ข้ามเมื่อ PENDING="?"
- **⚠ ต้อง re-verify:** "BIBLE branch drift" 3 branches (343/312/323 unmerged) ใน CRITICAL ALERTS **น่าจะ shallow-distorted เหมือนกัน** — ควร `git fetch --unshallow` แล้วนับใหม่ก่อนตัดสินใจ merge/discard
- **Durable fix (ฝั่ง env):** ตั้ง setup script ของ web environment ให้ `git fetch --unshallow origin` ตอน container เริ่ม → กัน distortion ที่ต้นเหตุ (hook guard = safety net ใน repo)
