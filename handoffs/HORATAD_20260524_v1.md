# HORATAD — Session Handoff
# Date: 2026-05-24 (session 1 ของวัน)
# Previous: handoffs/archive/HORATAD_20260523_v4.md

## STATE
App version : V3.3.23 (main + deployed) — ไม่ bump (engine.js เพิ่ม export, ไม่กระทบ UI)
Backups     : backup/v3.3.16–v3.3.22-main, backup/v24-matchRulesV24 (commit 6512c52)

## DONE (session นี้ 2026-05-24 v1)
✓ **buildNatalState() + matchRulesV24() ใน v3/engine.js** ⭐
  - `buildNatalState(pos, vel?, ascSignOverride?)` → planet state map
    - คำนวณ sign/quality/qualScore/house/lagnaAsp/strength ต่อดาว 1-10
    - lagnaAsp: KUM(h1)/LENG(h7)/YOK(h4,10)/TRI(h5,9)/NONE
    - ascSignOverride: ส่ง natal.ascSign เพื่อ build transit state
  - `matchRulesV24(natalState, rules, transitState?)` → matched rules[]
    - รองรับ NATAL_ATOMIC, NATAL_COMBINATION, TRANSIT_NATAL
    - ข้าม case_study อัตโนมัติ
    - DEFINITION/PRINCIPLE/REFERENCE (planet_ids=[]) → include เสมอ
  - ทดสอบกับ kb_v24-3.json 290 rules → match 131 rules สำหรับ 4 lagna-asp planets
  - ES module export (CSP-safe) — อยู่ใน v3/engine.js เท่านั้น ไม่แตะ script.js

✓ **GUARD T-05: validate_inputs()** ใน `v3/engine.js` `get_data()` — done
    - ตรวจ d(1-31)/m(1-12)/y(finite)/hr(0-47)/mn(0-59)/lng(-180..180) ก่อนส่ง _core()
    - throw RangeError พร้อม bad value — ทดสอบ 7 cases ผ่านหมด
    - interpretation.js ไม่แตะ — รับ pos[] จาก get_data() ที่ผ่านแล้ว

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

⚠️ **autonomous tasks หมดแล้ว** — งานที่เหลือทั้งหมดรอ user หรือ dependency

[ ] **Phase 2 Step 1 — core/ Tier 1 split** — ⚠️ ไม่แนะนำทำตอนนี้
    เหตุผล: ขึ้นต่อ CSP enforce (P2-A) — แนะนำรอ Lighthouse re-run + CSP 1 wk ก่อน

[ ] **Phase 2 Step 2 — db/ Tier 2 split** — same constraint as Step 1

[ ] **PROVINCES extract** — DEFERRED — ใช้ใน hot calc path

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ทดสอบ V3.3.23 บนมือถือ — KB async load + ทำนาย flow + race UX toast
[ ] [ทดลองใช้] ทดสอบ V3.3.22 บนมือถือ — M6 persist + nav "จร" compareMode===3
[ ] [ทดลองใช้] ทดสอบ V3.3.21 บนมือถือ — T1/T2 + transit unit popup + eventChart cycling
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [ทดลองใช้] Lighthouse mobile + desktop → `docs/cia/lighthouse_*` (วัดผล V3.3.23 -49%)
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy Option A/B/C → GUARD R-07
[ ] [BLOCKED] julian_all.json move target — รอ user เลือก
[ ] [BLOCKED] CSP enforce (P2-A) — รอ user ส่ง browser violations 1 wk Report-Only

## WHY LOG
- **matchRulesV24 ใน engine.js แทน script.js** — CSP-safe ES module ตั้งแต่แรก ไม่ต้องแก้ 2 รอบ; engine.js solid แล้ว (ไม่ redesign logic คำนวณ)
- **ascSignOverride param** — transit state ต้องใช้ natal ascendant เป็น reference ในการนับ house → buildNatalState() ทำได้ทั้งสองกรณีด้วย param เดียว ไม่ต้องสร้าง buildTransitState() แยก
- **DEFINITION/PRINCIPLE/REFERENCE include เสมอ** — rules เหล่านี้ไม่มี planet trigger, เป็น background context ที่ใช้ทุกดวง → matching ที่ถูกต้องคือ include all
- **ไม่ bump version** — engine.js เพิ่ม export functions ใหม่ ไม่แตะ UI หรือ SW cache → ไม่มี breaking change ไม่ต้อง bump
