# JULIAN — Session Handoff
# Date: 2026-05-25 (session 1 ของวัน)
# Previous: handoffs/archive/JULIAN_20260524_v1.md

## STATE
Records       : 63,804 (≈58% of 100K target — workflow รันต่อเอง)
Accuracy      : A=0 B=0 **C=616** D=63,168 F=10 — รอ backfill ลด C ลงไป D ที่เหมาะสม
Distribution C: 588 from wikidata, 28 from wikipedia_th, 0 from astrotheme
Sandbox quirk : Wikidata SPARQL = 403 (allowlist) → ต้องผ่าน GHA workflow
session_scope : ✅ gitignored แล้ว (commit 118effa main) — race condition จบ

## DONE (session นี้)

### Architectural fix (cross-cutting, BIG scope งานแต่ทำเสร็จใน session นี้)
✓ **commit 118effa** — gitignore `.claude/hooks/session_scope`
   - ปัญหา: session_scope commit ลง repo → multiple sandboxes race + merge conflict
   - แก้: `.gitignore` + `git rm --cached` → file ยังอยู่ local ไม่ track
   - hook ทน missing file (`[ -f "$SCOPE_FILE" ]` line 55 session-start.sh)
   - **payback ทุก session ต่อจากนี้** — ไม่มี race อีก
✓ **branch force-update** drop commit ลอย 29d12a7 (origin/claude/julian-NcORR sync กับ main)

### JULIAN deliverable
✓ **commit ba574a7** — `workers/julian_backfill_accuracy.mjs` (151 lines)
   - read julian_all.json filter accuracy=C && source=wikidata:Qxxx
   - batch 50 QIDs → SPARQL `psv:P569 wikibase:timePrecision`
   - precision >= 14 → keep C, < 14 → downgrade D + confidence 0.85 + notes
   - default dry-run, `--apply` เขียนทับ, `--limit N` ทดสอบ batch เดียว
   - validates: syntax OK + sandbox tests structure (real run = GHA)
✓ **commit 9184fb9** — append `📨 Incoming from JULIAN` ใน GUARD handoff
   - yaml template สำหรับ workflow_dispatch ครบ
   - inputs: apply (bool), limit (string)

### Cross-project follow-through (GUARD scope ใน session เดียวกัน)
✓ **commit b3bb1b5** — `.github/workflows/julian_backfill.yml`
   - one-shot manual trigger ตาม template ที่ส่งให้
   - auto-commit + push เมื่อ apply=true

## PENDING — 🟢 Claude ทำเองได้ (sandbox)
[ ] Phase 3 `workers/julian_validate.mjs` — เปรียบ empirical vs Wikidata ค้นพบ contradictions (รอ raw bucket accumulate, post-GUARD Phase 4)
[ ] Wikipedia TH parser refinement — เพิ่ม pattern ใหม่จาก next workflow run (รอ scan run ใหม่)
[ ] Astrotheme parser fix — เคย deprioritized เพราะใช้ Wikidata coord enricher แทน (ดู memory line)

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]
[ ] [ทดลองใช้] ⭐ **trigger JULIAN Backfill Accuracy workflow** (1 click):
   1. https://github.com/horatad/horatad/actions/workflows/julian_backfill.yml
   2. Run 1: apply=false → ดู summary (Keep C / Downgrade D / Not found)
   3. Run 2 (ถ้า summary ดู): apply=true → auto-commit julian_all.json
[ ] [ทดลองใช้] ⭐ `tools/julian_seed_input.html` — กรอกคนใกล้ตัว accuracy A/B (ยังไม่มี A เลย)
[ ] [ทดลองใช้] ทดสอบ HORATAD "ดาวน์โหลดข้อมูลสาธารณะ" — verify schema ยังใช้ได้
[ ] [BLOCKED] Phase 4 architecture — GUARD ต้อง patch `julian_sync.yml` ให้ `git add data/julian_raw/*.jsonl` (request 2026-05-24 ยังค้าง)

## DEFERRED — รอ "รอบใหญ่" / dependency
[ ] Add `birthPrecision` field ใน julian_all.json schema — จะช่วยให้ backfill ครั้งต่อไปไม่ต้อง re-query Wikidata
   - ต้อง update scraper.mjs เก็บ field + migration script
   - effort medium → ทำเฉพาะถ้าเจอ scenario ที่ต้อง backfill ซ้ำ
[ ] Refactor 3 scrapers ให้ใช้ shared `appendRaw` pattern เดียว — ตอนนี้ pattern ซ้ำ

## WHY LOG
- **gitignore session_scope** — race condition กินเวลา session 50% (rebase conflict + manual resolve loop)
  ผลที่ user สัมผัส: session ต่อไปไม่เสียเวลาแก้ scope file อีก fresh sandbox + parallel sessions ทำงานได้แยกกัน
- **Backfill = standalone script ไม่ใช่ workflow inline** — เผื่อ user รัน local ได้ถ้ามี env ที่เข้า Wikidata ได้
  (Workflow yaml เป็นแค่ wrapper trigger) + test ง่ายกว่า + reusable
- **Cross-project handoff request before workflow** — เผื่อ user เลือก auto-commit logic ต่าง / รวมเข้า workflow อื่น
  แต่ session ต่อกัน → switch scope GUARD ในเวลาเดียว ทำเองเลย (1 commit เล็ก)
- **ไม่ลบ time_utc ตอน downgrade** — ข้อมูลไม่หาย แค่ flag accuracy=D ให้ downstream filter ได้
  (เผื่อ later analysis ของ "Wikidata round-hour bias" หรือ verification ผ่าน external source)
