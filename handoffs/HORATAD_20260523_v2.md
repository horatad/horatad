# HORATAD — Session Handoff
# Date: 2026-05-23 (session 2 ของวัน)
# Previous: handoffs/archive/HORATAD_20260523_v1.md

## STATE
App version : V3.3.22 (main + deployed)
Backups     : backup/v3.3.16–v3.3.22 (v3.3.22 พึ่งสร้างใหม่บน main — มี XSS patch + M6 ครบ)

## DONE (session นี้ 2026-05-23 v2)
✓ **Doc drift fix** — PROJECT_STATUS.md sync (V3.3.19→V3.3.22, M1-M6 ✅ แทน M4-M6 pending)
✓ **M6 port** — localStorage persist บน main ปัจจุบัน (ไม่ rescue จาก backup เพราะ XSS conflict)
  - `_moveCursorByFixed()` → save `horatad_transit_cursor` หลัง `_transitCursor=next`
  - `_transitNextSignChange()` → save `horatad_transit_cursor` ตอนเจอ sign change
  - `cycleMemory()` compareMode===1 → save `horatad_synastry_idx`
  - `cycleMemory()` compareMode===2 → save `horatad_event_idx`
  - `_tsCalc()` → set `_transitCursor={d,m,y_be,t}` + save (sync จาก DOM inputs)
  - `_initApp` (DOMContentLoaded) → restore ทั้ง 3 keys หลัง `_nowStr()` init
✓ **`_updateNavHeader()` compareMode===3** — เพิ่ม branch "จร · d/m/y · t" จาก `_transitCursor`
✓ **Version bump** V3.3.21 → V3.3.22 (script.js / index.html / sw.js / style.css / version.json / manifest no version)
✓ **CHANGELOG** เพิ่ม entries V3.3.20/21/22
✓ **Phase 2 prep — module boundary audit** — `docs/HORATAD_modules.md`
  - 7 tiers, 30+ proposed modules ใน core/db/render/ui/io/interpretation/calc
  - **Quick win หลัก**: KB_RULES บรรทัด 97 = 49.4% ของ script.js → extract เป็น `v3/kb_embedded.json` ลด script.js 50% ทันที
  - Risk register, dependency map, build step trade-off (recommend ES modules ไม่ใช้ bundler)
  - Order: CSP enforce (P2-A) → code-split (P2-C) — ห้ามสลับ (inline event handler scope)

## PENDING — 🟢 Claude ทำเองได้ (sandbox)
[ ] **Phase 2 Step 0 — KB_RULES extract** — ⭐ HIGH VALUE: ลด script.js 50% (198KB → JSON file). Spec: `docs/HORATAD_modules.md` §1, §5. ทำได้ก่อน CSP enforce
[ ] **Phase 2 Step 1 — core/ Tier 1** — pure logic split (engine/lunar/varga/transit). Spec: `docs/HORATAD_modules.md` §2 Tier 1
[ ] **julian_all.json move** — รอ user เลือก target (Release asset / separate repo / sharded)
[ ] **CSP enforce (P2-A)** — รอ user ส่ง CSP violations หลัง Report-Only 1 wk

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]
[ ] [ทดลองใช้] ทดสอบ V3.3.22 บนมือถือ — M6 persist + nav header (จร) + refresh test
[ ] [ทดลองใช้] ทดสอบ V3.3.21 บนมือถือ — T1/T2 + transit unit popup + eventChart cycling
[ ] [ทดลองใช้] ทดสอบ NOK TTS guide บน Android Chrome
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [ทดลองใช้] Lighthouse mobile + desktop → save to docs/cia/lighthouse_*
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy — รอ Option A/B/C → GUARD R-07 (recommend C)

## 📨 Incoming from GUARD
- F1 XSS ✅ patched (44 sites ใน script.js — _escHtml() ทั้งหมดยังอยู่หลัง M6)
- horatad-auth gate: CSS-only bypass → รอ user ตัดสิน intent (security vs UX friction)
- R-08 LCP: รอ Lighthouse report
- CSP Report-Only: รอ 1 wk → enforce

## WHY LOG
- **M6 ทำซ้ำบน main แทน rescue จาก backup/v3.3.22** — ตามคำแนะนำ handoff ก่อนหน้า เพราะ backup branch ไม่มี XSS patch ที่ apply หลังจากนั้น. การทำใหม่บน main ทำให้ XSS patch ติดอยู่ครบ + M6 ใช้งานได้ → security regression = 0
- **`_updateNavHeader()` compareMode===3 เป็น new branch** — ก่อนหน้านี้ตกใน else (natal info) ซึ่งทำให้ผู้ใช้สับสนตอน cycle transit cursor ว่ากำลังดูอะไร ตอนนี้แสดง "จร · d/m/y · t" ชัดเจน
- **Persist เฉพาะ index ไม่ใช่ object** — synastry/event มี db backing อยู่แล้ว save แค่ index พอ (lightweight + กัน stale data ถ้า db เปลี่ยน). transitCursor ต้อง save object เพราะไม่มี db backing
- **doc drift discovered** — handoff HORATAD_20260523_v1 อ้างว่า M4/M5/M6 ยังไม่ได้ทำ ที่จริง M4/M5 อยู่บน main แล้ว (commits 766de9c, f75a137) แต่ไม่ได้อัปเดต doc → BIG session ถัดไปควรตรวจ drift นี้ก่อน assume handoff state
- **Phase 2 prep แทน actual refactor** — Phase 2 P2-C (code-split) ขึ้นต่อ P2-A (CSP enforce) ที่ blocked on user 1-wk observation. ทำ audit doc ก่อนเพื่อให้ session ถัดไปไม่ต้อง re-discover boundaries
- **KB_RULES = quick win อันดับ 1** — บรรทัด 97 inline 198KB คือ 49.4% ของ script.js. extract เป็น JSON file ลด bundle 50% โดยไม่ต้อง refactor logic. ทำได้ก่อน CSP enforce
- **Build step recommendation = B (ES modules)** — ไม่ใช้ bundler เพราะ project ขนาดนี้ overkill + เพิ่ม CI dependency. ES modules พอ + ใช้ `window.X = X` shim สำหรับ `onclick="..."` ที่มีอยู่
