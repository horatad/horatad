# HORATAD — Session Handoff
# Date: 2026-05-23 (session 2 ของวัน — BIG+HORATAD)
# Previous: handoffs/archive/HORATAD_20260523_v1.md

## STATE
App version : V3.3.21 (main + deployed)
Backups     : backup/v3.3.16–v3.3.22 (v3.3.20=M4, v3.3.21=M5 — ทั้งคู่อยู่ใน main แล้ว)

## DONE (session นี้)
✓ **M4** — rename natal1→natal / natal2→synastry ทั้ง script.js (V3.3.20)
  - natal1→natal (44 occurrences), natal2→synastry (24 occurrences)
  - ลบ let natal2=null; duplicate (synastry มีจาก M1 แล้ว)
  - GUARD XSS _escHtml() 44 sites คงอยู่ครบ
✓ **M5** — eventChart full support + compareMode-aware rendering (V3.3.21)
  - outerChart = compareMode===1?synastry : compareMode===2?eventChart ใน _redraw()/drawChart()
  - buildCompareReport() + outerLabel param
  - cycleMemory() แยก mode 1 (_synastryIdx) / mode 2 (_eventIdx+eventChart)
  - _eventSlotLoadRecord() → eventChart + _compareMode=2
  - _sompongLoadByUid(), _groupLoadAsNatal2(), calc2 → _compareMode=1
  - _updateNavHeader() compareMode-aware

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

[ ] **M6** — localStorage persistence
    - _synastryIdx, _eventIdx, _transitCursor บันทึกลง localStorage ทุกครั้งที่เปลี่ยน
    - key: 'horatad_synastry_idx', 'horatad_event_idx', 'horatad_transit_cursor'
    - restore ตอน DOMContentLoaded หลัง _transitCursor=_today()
    - _transitCursor ต้อง normalize เป็น y_be เสมอ (ดู WHY LOG เดิม)

[ ] **_updateNavHeader() info text** — ปรับตาม compareMode=3 (transit date)
    compareMode=3 → แสดง transit date จาก _transitCursor แทนชื่อดวง

[ ] **_tsCalc() ← sync กับ _transitCursor** (script.js:~3060)
    เมื่อกด "คำนวณจร" → อัปเดต _transitCursor จาก input fields

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ทดสอบ V3.3.21 บนมือถือ — eventChart mode, cycleMemory แต่ละ mode
[ ] [ทดลองใช้] ทดสอบ V3.3.19 บนมือถือ — T1/T2 buttons + transit unit popup
[ ] [ทดลองใช้] ทดสอบ V3.3.17 บนมือถือ — memory modal UX ใหม่ + sort buttons
[ ] [ทดลองใช้] ทดสอบ NOK TTS guide บน Android Chrome
[ ] [ทดลองใช้] CF: deploy horatad-ai Worker → GUARD T-02
[ ] [ทดลองใช้] Export horatad-auth + horatad-ai Worker source → GUARD T-06
[ ] [BLOCKED] cloud sync — รอ server confirm
[ ] [BLOCKED] QR URL privacy — รอ Option A/B/C → GUARD R-07

## 📨 Incoming from GUARD
- F1 XSS ✅ patched (7 sites) — M4/M5 maintain ครบแล้ว
- horatad-auth gate: CSS-only bypass → ตัดสินใจ intent (security vs UX friction)
- R-08 LCP: รอ Lighthouse report จาก user

## WHY LOG
- **M4 ทำซ้ำแทน rescue จาก backup** — backup/v3.3.22 ไม่มี GUARD XSS patch ซึ่ง apply ทีหลังบน branch ต่างกัน ทำ M4 ใหม่บน main ปลอดภัยกว่า XSS ติดตามมาอัตโนมัติ
- **M5 outerChart pattern** — ใช้ local const `outerChart` แทนอ้าง `synastry` global โดยตรง ทำให้ M6/M7 เพิ่ม mode ใหม่ได้ใน 1 จุด
- **_synastryIdx ใช้ index แทน find-by-key** — เดิม cycleMemory หา index จาก key ทุกครั้ง ใช้ _synastryIdx ตรงๆ เร็วกว่าและ consistent กับ _eventIdx pattern
