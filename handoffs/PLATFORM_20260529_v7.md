# PLATFORM — Session Handoff
# Date: 2026-05-29 (session 7 ของวัน — BIG + PLATFORM strategy deep-dive)
# Previous: handoffs/PLATFORM_20260529_v6.md

## STATE
Branch  : claude/modest-dijkstra-lOBxQ → ff main ✅ (135a86a)
Pipeline: inbox → scheduled → posted · autopost 08:30 ICT · ทำงานปกติ
Scope   : session นี้ทำ brand strategy + CLAUDE.md standing rules (ไม่ได้แตะ code)

---

## DONE (session นี้ — 2026-05-29 session 7)

✓ **เปลี่ยนการเรียกผู้ใช้ → "พี่ปีเตอร์"** — CLAUDE.md 2 จุด (ชื่อเล่น + กฎเรียก)

✓ **Brand North Star section ใน CLAUDE.md** (standing rule — ทุก session อ่านก่อนเริ่ม) ครอบคลุม:
  - Tagline 🟡 PROVISIONAL: "โหราศาสตร์ที่ตรวจสอบได้"
  - ICP: ผู้แสวงหาที่ไร้บ้านทางความเชื่อ
  - Positioning statement (Moore template)
  - ตาราง "เราคือสะพาน"
  - หลักการแกน "ความซื่อสัตย์ = ตัวสินค้า"
  - FIT metric (คุณภาพ comment ไม่ใช่ยอด)
  - Content engine "โหราทาส Investigates" + 3 hard gate

✓ **Session Report ครบถ้วน** — `docs/PLATFORM_STRATEGY.md` section ท้าย "📋 Session Report 2026-05-29"
  - 6 หัวข้อใหญ่: mix-up root cause · FB measurement framework · audience strategy · โหราทาส Investigates · brand north star · unresolved paradox

---

## PENDING — 🟢 Claude ทำเองได้ (sandbox)

### ต่อจาก v6 (ยังไม่ได้ทำ)
[ ] **cohort builder สำหรับ "โหราทาส Investigates"** — งาน JULIAN
    รับประเภทเหตุการณ์ → ดึงกลุ่มคนที่เจอแบบเดียวกัน + control group อัตโนมัติ
    → เปิด session JULIAN ทำต่อ

[ ] **FB Insights Phase 0 demand signal** — งาน PLATFORM
    Google Trends / YouTube keyword search (WebSearch ได้) สำหรับ intent demand
    คีย์เวิร์ดสายตรรกะ (สุริยยาตร์, ผูกดวงเอง, เรียนโหราศาสตร์) vs สายมู

[ ] **`gen_project_status.mjs`** — งาน BIG
    generate PROJECT_STATUS.md จาก handoffs แทนแก้มือ → drift หายถาวร

---

## PENDING — 🔴 [ทดลองใช้] / [BLOCKED]

[ ] [ทดลองใช้] ต่อจาก v6: EP02 รอบถัดไปบน Page — ยืนยันสายตา caption สะอาด + การ์ดขึ้น
[ ] [ทดลองใช้] ต่อจาก v6: ทดสอบ F1 trigger workflow fail → ตรวจว่า issue ถูกสร้าง
[ ] [ทดลองใช้] ต่อจาก v6: ทดสอบ fb_post_manage จริง (Actions → edit post)
[ ] [ทดลองใช้] fb_post_helper backlog section แสดงถูก
[ ] [ทดลองใช้] tagline สุดท้าย — รอพี่ปีเตอร์ตกผลึก paradox "ตรวจสอบได้" vs "prediction claim"
[ ] [ทดลองใช้] เพิ่ม `read_insights` scope ตอน regenerate token เมื่อ followers ≥100

---

## DEFERRED — รอ decision / dependency

[ ] **FB Insights own-page loop** (Phase 1-3) — เลื่อนออก รอ followers โตพอ
    เมื่อพร้อม: `fb_insights.yml` (Actions) + `fb_analyze.mjs` (local analysis)
    Architecture: cron เก็บ → Claude วิเคราะห์ local (decoupled, 0 round-trip)
[ ] **Combined post** (คลิป YouTube + วิจัย JULIAN ใน 1 โพสต์) — รอ BIG วางลำดับก่อน
    แบบ A: findFBCaption (statistical) + คลิป key finding
    แบบ B: buildResearchCaption (BTS) + คลิป background
[ ] F6 cron ordering: youtube_sync ย้ายก่อน 08:00 — ยังรอ (lag 1 วันไม่พัง)

---

## 🧭 Brand Strategy — สรุป key decisions (อ่านก่อนทำ content ทุกครั้ง)

**ทั้งหมดนี้บันทึกถาวรใน CLAUDE.md section "🧭 Brand North Star" + `docs/PLATFORM_STRATEGY.md` section "📋 Session Report 2026-05-29"**

| เรื่อง | ตกผลึกแล้ว | ยังรอ |
|---|---|---|
| ICP | ✅ ผู้แสวงหาที่ไร้บ้านทางความเชื่อ | — |
| หลักการแกน | ✅ ความซื่อสัตย์ = ตัวสินค้า | — |
| FIT metric | ✅ คุณภาพ comment ไม่ใช่ยอด | — |
| Flagship format | ✅ "โหราทาส Investigates" + 3 hard gate | JULIAN cohort builder |
| Tagline | 🟡 "โหราศาสตร์ที่ตรวจสอบได้" (provisional) | paradox ตกผลึก |
| Measurement | 🟡 framework พร้อม | รอ followers ≥100 |
| Enemy framing | ✅ "หมอดู/มูเตลู" ไม่แตะศาสนา | — |

**Unresolved paradox (เก็บไว้ — ไม่เร่ง):**
โหราศาสตร์ claim หลัก = พยากรณ์อนาคต · แต่ "ตรวจสอบได้" คือมาตรฐานที่โหราศาสตร์ส่วนใหญ่สอบตก
ทิศทางคลี่: (1) ตรวจสอบ = method ไม่ใช่ guarantee (2) prediction = probabilistic ไม่ใช่ deterministic (3) คุณค่า = ความหมาย + ส่วนที่รอดการกรอง

---

## WHY LOG

- **Brand North Star ลง CLAUDE.md ไม่ใช่ handoff (2026-05-29)** — handoff expire ทุก session · CLAUDE.md = ทุก session อ่าน = standing rule ที่ถูกที่สุดสำหรับ brand decision ระดับนี้

- **copy เพจมู = สัญญาณผิด (2026-05-29)** — เพจมูเสิร์ฟ audience สายมู/intuition ซึ่งขัด content policy เรา · optimize ตามเขา = ดึงคนผิดกลุ่ม = engagement ขึ้น แต่ทำลาย positioning · FIT metric (คุณภาพ comment) สำคัญกว่า reach ตอน stage เล็ก

- **"โหราทาส Investigates" ใช้ 3 hard gate (2026-05-29)** — n เล็กหา pattern โดยบังเอิญเสมอ · ไม่มี control group = post-hoc mua ที่เราเองรังเกียจ · ความกล้า "เจอ pattern พอกัน" โพสต์ความจริงนั้นคือ differentiator จากเพจมู

- **เลื่อน own-page insights loop รอ followers โตพอ (2026-05-29)** — วัด n=5 followers = noise ไม่ใช่ข้อมูล · Phase 0 = demand signal ภายนอก (intent keyword, FIT metric) คุ้มกว่าตอนนี้

- **tagline provisional (2026-05-29)** — "ความเชื่อที่ตรวจสอบได้" แตะศาสนา → เปลี่ยนเป็น "โหราศาสตร์ที่ตรวจสอบได้" · แต่ยังมี paradox prediction claim vs falsifiability ที่ยังไม่คลี่ → mark provisional รอตกผลึก ไม่บังคับฟันธงก่อนพร้อม

---

## 📨 Incoming from GUARD (2026-05-29) — FB pipeline security audit

GUARD audit FB content automation pipeline ครั้งแรก (SOP-03 new-feature review) → `docs/cia/fb_pipeline_audit_2026-05-29.md`

**ผลรวม: ผ่านเกณฑ์พื้นฐาน ✅ ไม่มี critical/P0** — secrets อยู่ใน GitHub Secrets, ไม่มี hardcoded token, ไม่มี pull_request trigger ที่ fork ดูด secret ได้, idempotency/dedup + destructive guardrails ครบ

**สิ่งที่ GUARD แก้ให้แล้ว (workflow = GUARD owns):**
- `fb_post_manage.yml` — เพิ่ม `permissions: contents: read` (least-privilege destructive workflow, R-21)

**ขอ PLATFORM พิจารณา (worker code = PLATFORM owns):**
- **R-22 (P3 hardening)** — `recentlyPostedMessages` (fb_autopost), `fb_kpi_report`, `fb_post_manage` ใส่ token ใน query string. GHA mask เฉพาะ literal secret — encoded form อาจหลุด mask ถ้าเผลอ log url. ความเสี่ยงจริงต่ำ (FB token alnum → encode no-op) แต่แนะนำ:
  - ใช้ `Authorization: Bearer <token>` header หรือ POST body แทน token-in-query ทุก GET (autopost POST ทำถูกแล้ว)
  - ห้าม `console.log(url)` ที่มี token
- ไม่เร่ง — P3 hardening ทำตอนแตะไฟล์เหล่านี้รอบหน้าได้

**Informational (ไม่ต้องทำ):**
- R-23 — Drive→inbox = content supply chain vector; fb_policy gate + cap 14/wk คุมพอสำหรับ single-user ตอนนี้
